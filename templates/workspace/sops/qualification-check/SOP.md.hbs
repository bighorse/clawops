{{#if qualification.enabled}}
# 资质检查 SOP (qualification-check)

基于企业画像全面梳理当前持有资质的状态与到期预警，并由 LLM 直接完成资质匹配分析和新资质申报建议，最终写入到期提醒并推送小程序卡片。

> ⚠️ **后端依赖**
> - `GET {{{qualification.api_base}}}{{{qualification.enterprise_profile_path}}}` — 取企业工商+资质画像
> - `GET {{{qualification.api_base}}}{{{qualification.qualification_check_path}}}` — 从天眼查同步专利/商标/证书/荣誉明细（含到期日）
> - `POST {{{qualification.api_base}}}{{{qualification.save_result_path}}}` — 写资质分析结果，触发小程序卡片推送
> - `POST {{{sop_webhook.qualification_reminder_url}}}` — 向 clawops 注册到期提醒（cron 任务按时发短信/webhook）
>
> **资质分析由本地 LLM（本身）完成，无外部 AI 调用。**

---

## Inputs

`sop_execute('qualification-check', payload={...})` 期望的 payload:

- `enterprise_name` (string, **优先级最高**): 用户消息中明确指定的企业名
- `openid` (string, 必填): 当前用户 openid

**enterprise_name 兜底链**（按优先级）:
1. payload 里明确传入的 `enterprise_name`
2. `memory_recall(query="user_profile_company_name")`
3. IDENTITY.md 里渲染的企业全称
4. 都没有 → narrate "请告诉我要查哪家企业的资质信息" 并 `sop_advance status=Failed` 终止

## 工作目录

所有产物落到 `case/qualification-check/<enterprise_safe>/`：

- `enterprise_safe` 规范化：取企业全称，把 ` ` `/` `:` `*` `?` `"` `<` `>` `|` 替换成 `_`，其余（含中文）保留
- Step 1 开始时 narrate `enterprise_safe="..."` 并后续严格复用

---

## Steps

### Step 1：取企业基础画像

- tools: http_request, file_write, memory_recall
- notes:
  (1) 按兜底链确定 `enterprise_name`；全部失败则 `sop_advance status=Failed`。
  (2) 计算 `enterprise_safe`，narrate `enterprise_name="..."` 和 `enterprise_safe="..."`。
  (3) 调用接口，**必须带 fields 参数**（否则响应 73 KB 撑爆 context）：
      ```
      http_request GET {{{qualification.api_base}}}{{{qualification.enterprise_profile_path}}}
        ?enterprise_name=<enterprise_name>
        &agent_user_info={{sop_webhook.owner_openid}}
        &fields=enterprise_id,qualification_enterprise_id,enterprise_name,basic_info,qualification_info
      ```
      期望 `data` 含：`enterprise_id`、`qualification_enterprise_id`、`basic_info`（工商信息）、`qualification_info`（专利/商标/资质证书/荣誉摘要文本）。
  (4) 落盘 `case/qualification-check/<enterprise_safe>/profile.json`（直接保存 `data` 对象）。
  (5) 记下 `enterprise_id` 和 `qualification_enterprise_id`，narrate 出来，后续严格复用。
  (6) 降级：4xx/5xx → narrate "企业画像服务暂时不可用，请稍后再试" + `sop_advance status=Failed`。
  (7) `data` 为 `null` 或 `enterprise_id` 缺失 → 同上 Failed，**不要**自己捏造 id。

### Step 2：从天眼查同步完整资质明细

- tools: http_request, file_write
- notes:
  (1) 调用后端天眼查同步接口，拉取带到期日的全量资质数据：
      ```
      http_request GET {{{qualification.api_base}}}{{{qualification.qualification_check_path}}}
        ?enterprise_id=<enterprise_id>
        &agent_user_info={{sop_webhook.owner_openid}}
      ```
      期望响应 `data` 含 6 类字段数组：
      - `patents[]`：每条含 `title`, `reg_no`, `status`, `apply_date`, `valid_date`, `annual_fee_date`
      - `trademarks[]`：每条含 `title`, `reg_no`, `status`, `valid_end_date`
      - `certifications[]`：每条含 `title`, `reg_no`, `valid_end_date`
      - `honors[]`：每条含 `title`, `valid_end_date`（可为空）
      - `copyrights[]`：每条含 `title`, `reg_no`, `register_date`
      - `copyworks[]`：每条含 `title`
  (2) 落盘 `case/qualification-check/<enterprise_safe>/tyc_detail.json`（完整 `data` 对象）。
  (3) 若 `data` 为空或全部数组为空 → narrate "天眼查暂无该企业资质数据" 并**继续执行**（不中止，可能是新企业）。
  (4) 降级：4xx/5xx → narrate 原因 + 继续执行（Step 3 仍可基于 profile.json 里的 `qualification_info` 文字摘要分析）。

### Step 3：LLM 分析资质现状与申报建议（两阶段）

- tools: file_read, file_write
- notes:
  本步不调用任何外部 API，完全由本地 LLM 基于已有数据独立完成分析。拆成两次 LLM call 避免单次超时。

  #### 3a. 现状梳理 — 整理现有资质状态清单

  - file_read `profile.json`（取 `basic_info` + `qualification_info`）
  - file_read `tyc_detail.json`（取 6 类明细）
  - 基于上述数据，输出 **existing_qualifications 数组**，每项含：
    - `name`：资质名称
    - `type`：`PATENT` / `TRADEMARK` / `CERT` / `HONOR` / `COPYRIGHT`
    - `reg_no`：注册号/证书编号（无则 null）
    - `status`：`valid`（有效）/ `expiring_soon`（6个月内到期）/ `expired`（已过期）
    - `expiry_date`：到期/截止日（ISO date，无则 null）
    - `annual_fee_date`：专利年费截止日（仅 PATENT 类，无则 null）
    - `advisory`：一句话说明（到期提醒 / 已满足条件 / 建议动作）
  - 判断 `status` 规则：
    - `expiry_date` 距今 ≤ 180 天 → `expiring_soon`
    - `expiry_date` 已过 → `expired`
    - 其余 → `valid`
  - 落盘 `case/qualification-check/<enterprise_safe>/existing.json`（`{"existing_qualifications": [...]}` 格式）

  #### 3b. 申报建议 — 基于企业画像推荐可申请的资质

  - file_read `existing.json`（3a 产物，了解已有哪些）
  - 综合 `basic_info`（行业、注册资本、成立年限、营业范围）+ `qualification_info` 摘要 + `tyc_detail.json` 的数据，LLM 独立判断：
    - 企业当前达到或接近哪些资质认定门槛
    - 哪些资质对该行业/规模最有价值，企业尚未取得
  - 重点参考以下常见资质体系（按适用场景判断相关性）：

    **国家级认定类**
    - 高新技术企业：成立≥1年，研发费用占比≥3-5%，知识产权，技术人员≥10%
    - 专精特新"小巨人"：专精特新省级认定基础上，主营业务收入≥1亿或细分市场占有率高
    - 技术创新示范企业：在细分市场具有领先地位

    **省/市级认定类**
    - 专精特新中小企业：成立≥3年，主营业务收入2000万以上，研发费用占比≥3%
    - 科技型中小企业：注册≥1年，职工≥10人，年收入≤3亿，研发费用≥5万或≥总费用5%
    - 北京市"科小"认定：在北京注册，从事科技成果转化/研发

    **知识产权类**
    - 知识产权优势/示范企业：持有有效发明专利≥5件（优势）/≥10件（示范）
    - 贯标认证（知识产权管理规范 GB/T 29490）：有完整知识产权管理体系

    **质量/体系类**
    - ISO 9001：质量管理体系，适合大多数制造/服务企业
    - ISO 14001：环境管理，制造/工程类企业
    - CMMI（软件/IT企业）：软件过程成熟度模型

    **行业专项类**
    - 软件企业认定 + 软件产品登记：收入中软件占比≥30%
    - 双软认定：获得后可享受所得税优惠
    - 医疗器械注册证、ICP 许可、食品经营许可 等（按行业）

  - 输出 **recommended_qualifications 数组**，每项含：
    - `name`：资质名称
    - `type`：`CERT` / `PATENT` / `TRADEMARK` / `HONOR`
    - `match_level`：`HIGH`（强烈建议，基本满足条件）/ `MEDIUM`（部分满足，需补足）/ `LOW`（潜在方向，差距较大）
    - `rationale`：2-3句，说明企业哪些条件符合、哪些还差
    - `next_step`：最近一个可操作行动（如"本年度3月前提交省工信厅申报材料"）
  - 只输出 `match_level` 为 `HIGH` 或 `MEDIUM` 的建议（过滤掉 LOW）；最多输出 8 条
  - 落盘 `case/qualification-check/<enterprise_safe>/recommendations.json`（`{"recommended_qualifications": [...]}` 格式）

### Step 4：计算到期提醒日期并注册

- tools: file_read, http_request
- notes:
  (1) file_read `existing.json`（Step 3a 产物），提取所有带日期的资质。
  (2) **提醒时间规则**：

      | 资质类型 | 提醒节点 | reminder_type |
      |---------|---------|--------------|
      | 专利年费（`annual_fee_date`） | 费用日前 90 天 | `PATENT_FEE` |
      | 专利年报（`annual_report_date`，若有） | 报告期前 14 天 | `PATENT_ANNUAL` |
      | 商标续展（`expiry_date`） | 到期前 180 天、90 天（各一条） | `TRADEMARK_RENEWAL` |
      | 资质证书续期（`expiry_date`） | 到期前 180 天、90 天（各一条） | `CERT_RENEWAL` |
      | 荣誉复审（`expiry_date`，若有） | 到期前 90 天 | `HONOR_RENEWAL` |

      **只设今天之后的提醒**（已过期的跳过）；同一资质同类型最多 2 条。
  (3) 调用 clawops 注册提醒：
      ```
      http_request POST {{{sop_webhook.qualification_reminder_url}}}
        Content-Type: application/json
        {
          "openid": "{{sop_webhook.owner_openid}}",
          "enterprise_name": "<enterprise_name>",
          "qualification_enterprise_id": <qualification_enterprise_id>,
          "reminders": [
            {
              "reminder_type": "CERT_RENEWAL",
              "qualification_title": "<name>",
              "reg_no": "<reg_no 或 null>",
              "remind_at": "2026-06-30T09:00:00+08:00",
              "event_date": "2026-12-31"
            }
          ]
        }
      ```
      `remind_at` 使用 ISO-8601 含时区 `+08:00`；若某资质无到期日则不生成该条。
  (4) 降级：提醒注册失败 → narrate 警告并继续（不中止 SOP）。

### Step 5：保存分析结果到后端

- tools: file_read, http_request
- notes:
  (1) file_read `existing.json` + `recommendations.json`。
  (2) 调用后端写库接口，触发小程序卡片推送：
      ```
      http_request POST {{{qualification.api_base}}}{{{qualification.save_result_path}}}
        Content-Type: application/json
        {
          "enterprise_id": <enterprise_id>,
          "enterprise_name": "<enterprise_name>",
          "agent_user_info": "{{sop_webhook.owner_openid}}",
          "existing_qualifications": <existing.json 里的数组>,
          "recommended_qualifications": <recommendations.json 里的数组>
        }
      ```
  (3) 降级：写库失败不中止，narrate 警告 + 继续 Step 6。

### Step 6：汇总结果回复用户

- tools: file_read
- notes:
  (1) file_read `existing.json` + `recommendations.json`，组织回复。
  (2) 回复格式（严格按下方模板）：

      ---
      **当前资质状况（共 N 项）**

      ✅ 有效中
      - 高新技术企业（GF2024XXXXXX）— 有效至 2026-12-31
      - 发明专利：XX 技术（ZL202XXXXXXXX）— 年费截止 2026-09-01

      ⚠️ 即将到期（6 个月内）
      - 软件企业认定（RQ2022XXXXXXXX）— 2026-07-01 到期，**建议尽快启动续期**

      ❌ 已过期
      - ISO 9001 认证 — 2025-03-01 已过期，建议重新认定

      ---
      **建议申请的新资质（共 M 项）**

      🟢 强烈建议（HIGH）
      1. **专精特新中小企业** — <rationale 一句话> | 下一步：<next_step>

      🟡 可考虑（MEDIUM）
      2. **知识产权优势企业** — <rationale> | 下一步：<next_step>

      ---
      已设置 K 个到期提醒，届时将通过短信/消息提醒您。

      [查看资质详情](/pages/qualification/index?id=<qualification_enterprise_id 真实数字>)

  (3) 深链拼装：格式固定为 `/pages/qualification/index?id=<qualification_enterprise_id>`，把 `<qualification_enterprise_id>` 替换为 Step 1/5 拿到的实际数字。**注意**：不带 `.html`，路径开头有 `/`，例如 id=44 → `/pages/qualification/index?id=44`。
  (4) `sop_advance status=Done`。

---

## 自检（回答前必过）

1. `enterprise_id` / `qualification_enterprise_id` 是否来自本次 API 响应，非编造？
2. `existing.json` 里的 `status` 判断是否基于真实的 `expiry_date` 计算，非猜测？
3. `recommendations.json` 里是否已过滤掉 `match_level=LOW`？
4. `remind_at` 是否全部为今天之后？
5. 深链里 `{qualification_enterprise_id}` 是否已替换为真实数字？
6. FastGPT / 外部 AI 有没有被调用？**禁止**调用任何外部 AI 端点，分析必须由本 LLM 完成。

任一项 No → 回去修正再输出。

{{/if}}
