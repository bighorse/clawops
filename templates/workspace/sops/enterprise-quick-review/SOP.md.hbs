# 企业快评（Enterprise Quick Review）

为产业情报分析师提供一套标准化企业快速评估流程：先查内部科创企业数据库（13799 家），命中则用数据库信息填充基本面+科创属性、再以公开搜索补全融资/竞品/风险；未命中则走全公开搜索。从公司名消歧、缓存检查、数据库查询、多源检索，到成稿简报 + 分享卡片 + 最终汇报交付。全部检索可溯源、多源矛盾如实双口径标注、绝不编造数据。全程靠文件 handoff 传数据，断点可续跑。

## 依赖技能/脚本
- `scripts/query_enterprise_db.py`：查询内部科创企业数据库（需 MySQL 连接环境变量）
- `scripts/share_card.py`：生成分享卡片（需 pillow + qrcode + 中文字体）

## Inputs
- `company_name`：用户输入的公司名，允许自然口语（如 "快评：字节""查一下宁德时代""DeepSeek 这家公司怎么样"）。支持中英文、简称、品牌名、曾用名。

## Steps

1. **公司名消歧** — 确认官方注册全称、曾用名、品牌名/英文名，固定 `company_slug`。
   - tools: web_search, file_write, memory_recall
   - requires_confirmation: false
   - notes:
     • 【P0 强制：必须在发起任何业务检索前完成消歧】从用户输入中提取 `company_name`，用工商公开信息（天眼查/企查查/启信宝公开页面摘要）搜索公司官方注册全称，同时获取曾用名、品牌名/英文名。
     • 只有一家明确匹配 → 直接确认，写 `_disambiguation.md`，然后 `sop_advance` 进入 Step 2。不需要停下来问用户。
     • 遇到重名（如多个地区同名公司）或公开信息不足无法唯一确定时：**先用 narrate 把候选表格发到对话里**，格式如下——表格至少包含：序号、公司全称、所在城市、所属行业、匹配说明。末尾加「请输入序号或公司全称来确认」。**然后必须等用户在对话中回复**，根据回复的序号或全称确认目标公司。绝不自作主张选一家。等用户回复后方可 `sop_advance`。
     • 消歧确认后固定 `company_slug`：取公司英文名或拼音简化，全小写连字符，例 `bytedance`、`huawei`、`ningde-era`、`deepseek`。后续所有产物路径必须统一使用此 slug。
     • 将消歧结果写入 `briefs/<company_slug>/_disambiguation.md`，内容：官方注册全称、曾用名列表、品牌名/英文名列表、消歧依据（如工商来源 URL）。文件落盘后 file_read 确认非空。

2. **缓存检查** — 查该公司 24 小时内是否已有简报，有则问用户是否复用。
   - tools: shell
   - requires_confirmation: false
   - notes:
     • 【P0 强制：此步必须在任何检索之前执行，绝不跳过】执行 `ls briefs/<company_slug>/ 2>/dev/null` 或等效方式，查看该目录下是否存在 `brief_<YYYY-MM-DD>.md`，并获取文件的最后修改时间。
     • 若存在且距今 < 24 小时：计算距今小时数，**先用 narrate 向用户报告**「X 小时前查过（最新简报：brief_<日期>.md）。用缓存结果还是重新检索？」。**必须等待用户在对话中回复**后，根据回复决定：选「用缓存」→ 跳到 Step 7（交付）；选「重新检索」→ 继续 Step 3。
     • 无缓存或超过 24 小时 → 直接继续 Step 3，不需要停下来问用户。
     • 若 Step 2 未执行就进入 Step 3 或 Step 4 → 违反 P0，引擎必须拒绝并退回 Step 2。

3. **数据库查询** — 查内部科创企业数据库 `kcqy_zhxx`（13799 家企业），优先获取基本面与科创属性数据。
   - tools: shell, file_write
   - requires_confirmation: false
   - notes:
     • 【P0 强制：必须在公开网络检索之前执行】执行命令 `python3 scripts/query_enterprise_db.py "<公司官方全称>"`。该脚本内置三级匹配策略：精确匹配、LIKE 模糊匹配、关键词拆分匹配，返回 JSON。
     • 查询成功（`found: true`）→ 将 JSON 结果写入 `briefs/<company_slug>/_db_result.json`；然后在 `_structured_data.md` 中创建「基本面」和「科创属性」两个板块，数据来源标注为「科创企业数据库」。使用 Markdown 表格格式（表头：数据项、值、来源、来源日期），字段映射：gsmc→公司名称、ssdq→所属地区、ssly→所属领域、gsjj→公司简介、cptz→产品/技术特征、kjzz→科技资质。完成后进入 Step 4 时只需补搜融资历史、竞品对比、风险点三个板块。
     • 查询失败（`found: false`）→ 将结果写入 `briefs/<company_slug>/_db_result.json`（含 `found: false` 和说明）；narrate 告知用户「该公司未收录在内部数据库中，将走全公开搜索」。进入 Step 4 时按全部五个板块走公开检索。
     • 数据库连接失败（退出码 2）→ 写入 `_db_result.json` 含错误信息；narrate 告知用户「数据库暂时不可用，将走全公开搜索」；继续 Step 4，不阻断流程。

4. **多源检索与结构化** — 根据 Step 3 结果，补搜未覆盖板块，生成/补全 `_structured_data.md`。
   - tools: web_search, file_write, file_read
   - requires_confirmation: false
   - notes:
     • 【P0 强制：五板块全覆盖，不可只做部分】根据 Step 3 的 DB 查询结果决定检索范围：
       - DB 已命中：只需检索「融资历史」「竞品对比」「风险点」三个板块（基本面+科创属性已在 `_structured_data.md` 中，来自 DB）
       - DB 未命中/不可用：按全部五个板块检索——基本面（工商信息/注册资本/成立时间/法人实控人/股东结构/主营业务/规模/核心团队背景）、科创属性（专利数量与类型/核心专利方向/研发投入占比/研发团队规模/资质认证）、融资历史（轮次及时间/每轮金额与投资方/最新估值/IPO 进程）、竞品对比（同赛道主要竞品 3-5 家/各自定位规模差异/本公司差异化优劣势/市场份额排名）、风险点（经营风险/诉讼仲裁行政处罚/行业政策风险/市场技术替代风险）
     • 需检索的每板块用 ≥3 个检索词变体（中文简称+全称、英文名、加限定词如"估值""专利""竞品""风险""行政处罚"等组合），确保覆盖面充分。
     • 全部检索请求实时写入 `briefs/<company_slug>/_search_log.md`（在 Step 4 开始时创建该文件，若 Step 3 已创建则追加）。每条记录格式：`[HH:MM:SS] 板块: <板块名> | 检索词: "<检索词>" | 来源 URL: <URL> | 标题: <页面标题>`。每发起一条检索就追加一行，不要等全部完成再一次性写入。
     • 检索结果经核实后整理为结构化数据，追加/补全到 `briefs/<company_slug>/_structured_data.md`。格式为 Markdown 表格（表头：数据项、值、来源 URL、来源日期），每条数据必须含来源 URL 与来源日期（年月至少）。无来源的数据不得录入。DB 来源的数据源字段填「科创企业数据库」。
     • 任一板块确实无公开信息时，在该板块标题下写 `⚠️ 暂无公开信息`，绝不编造、绝不揣测。
     • 某板块有效信息点 < 3 条时：先换 3 组检索词重新搜索该板块；仍不足 3 条 → 在 `_structured_data.md` 该板块末标注「⚠️ 信息有限（仅 N 条有效信息点）」，并在最终汇报时如实告知用户、追问他是否愿意提供补充信息。

5. **跨源一致性检查** — 检查同一指标是否存在多源矛盾（含 DB 与公开来源之间）。
   - tools: file_read
   - requires_confirmation: false
   - notes:
     • 读取 `briefs/<company_slug>/_structured_data.md`，横向扫描同一数据项在不同来源中的取值。特别注意 DB 来源数据与公开搜索数据之间是否矛盾。对比标准：数值差距 > 20% 或定性结论相反视为"严重矛盾"。
     • 存在严重矛盾 → 调用 `lean-ontology-build` 引擎做本体核验，辅助判断哪个口径更可靠。DB 数据与公开数据矛盾时，优先以 DB 为记录基准、以公开数据为补充参考，并在简报中双口径注明。
     • 矛盾处理原则：在简报中必须双口径如实标注，绝不允许择一隐匿。标注格式示例：`出货量：GGII 口径 XXX 万台（2025Q3） vs 高工产业研究口径 YYY 万台（2025Q2）`。若有核验结论可在括号内附注「本体核验倾向于 A 口径」。
     • 无严重矛盾时可跳过本体构建，直接进入 Step 6。
     • 将矛盾核查结果追加写入 `briefs/<company_slug>/_structured_data.md` 末尾的「一致性检查」section，记录：对比的数据项、两个来源及各自值、矛盾判定（严重/轻微）、最终处理方式。

6. **成稿** — 撰写正式简报 `brief_<YYYY-MM-DD>.md`。
   - tools: file_read, file_write
   - requires_confirmation: false
   - notes:
     • 读取 `briefs/<company_slug>/_structured_data.md`（含一致性检查 section）、`briefs/<company_slug>/_db_result.json`（如有）和 `briefs/<company_slug>/_disambiguation.md`，整合撰写正式简报。
     • 简报写入 `briefs/<company_slug>/brief_<YYYY-MM-DD>.md`，严格按固定结构：一级标题为「<公司官方全称> — 企业快评」；元信息行标注简报日期、数据来源（科创企业数据库 + 公开网络检索）、检索截止日期；正文依次为「一句话画像」（≤50 字概括定位/阶段/特征）、「一、基本面」（DB 命中时优先引用 DB 数据）、「二、科创属性」（DB 命中时优先引用 DB 的 kjzz/gsjj/cptz）、「三、融资历史」（按时间从早到晚排列）、「四、竞品对比」（至少列出 2-3 家有据可查的竞品）、「五、风险点」；最后为「快速总结」（150-300 字，有观点和判断，指出最突出的竞争优势 + 最值得关注的 2-3 条风险，给出"可投/观望/回避"倾向判断）；末尾附声明「本简报基于科创企业数据库与公开网络检索生成，与内部专业数据库存在差距，仅供内部参考，不构成投资建议。」
      • 【P0 强制：每条数据必须逐条标注来源，不可集中罗列】简报中每条数据（特别是来自 `_structured_data.md` 中标记为「科创企业数据库」的条目）必须在同一行或紧跟行标注其来源。正确的格式是每条数据后跟 `（来源：科创企业数据库）` 或 `（来源：<来源名>，<YYYY.MM>）`。禁止的做法：在段落末尾集中写一句「来源：A、B、C」——这会让读者无法区分哪条数据来自数据库、哪条来自公开搜索。DB 来源的数据必须清晰可辨。
     • 某板块确实无公开信息时，在该板块标题下写 `⚠️ 暂无公开信息`。
     • 快速总结要敢于下判断，列举依据（引用前面五板块的具体条目），并给出倾向性结论。
     • 写完后用 file_read 自检：确认五板块标题齐全、关键数字均有来源标注、末尾声明已附。

7. **交付** — 发布简报下载链接、生成分享卡片、推送最终汇报、记录查询历史。
   - tools: publish_file, shell, memory_store
   - requires_confirmation: false
   - notes:
     • 发布简报下载链接：调用 `publish_file` 为 `briefs/<company_slug>/brief_<YYYY-MM-DD>.md` 生成签名下载链接（记作 `BRIEF_URL`）。
     • 生成分享卡片：从简报中提炼一句话定位（≤28 字，公司名 + 最核心的差异化标签）和 3 条重点（每条 ≤60 字，按信息量排序；至少 1 条必须是风险提示，且只能放在第二条或第三条、不能全堆在末尾）。确保卡片文案为纯结论摘要，严禁出现具体数据明细（如不写"估值 50 亿美元"而写"估值已进入独角兽梯队"），严禁任何利诱型表述。执行命令 `mkdir -p briefs/cards && python3 scripts/share_card.py --company "<公司官方全称>" --tagline "<一句话定位>" --p1 "<第一条重点>" --p2 "<第二条重点>" --p3 "<第三条重点>" --out "briefs/cards/<company_slug>_<YYYY-MM-DD>.png"`。执行成功后调用 `publish_file` 为卡片取得下载链接（记作 `CARD_URL`）。执行失败则检查 Python 环境和依赖（pillow、qrcode、中文字体路径、python3 白名单），narrate 具体错误信息，但不能因卡片失败而阻断整个交付。
     • 撰写并推送最终汇报（在对话中 narrate，总字数 ≤800 字），结构：一句话画像（取简报中的原句）、核心亮点（3-5 条简洁有力附来源线索）、主要风险（2-4 条诚恳点明不回避）、链接区（必须独立成段，两个链接各占一行，使用完整 Markdown 链接格式：`📄 [点击下载 企业快评简报](BRIEF_URL)` 和 `🖼️ [点击下载 分享卡片](CARD_URL)`）、灰色声明小字、追问引导。链接区必须是 `publish_file` 返回的真实签名 URL，不得使用文件路径占位。若任一链接缺失，必须在汇报中醒目说明原因并提供「补生成卡片」重试指令，不得悄无声息省略。
     • 记录查询：调用 `memory_store` 将本次查询写入记忆，key 格式 `brief:<company_slug>:<YYYY-MM-DD>`，value 记录公司官方全称、查询时间（ISO 8601）、数据来源（DB 命中/仅公开搜索）、简报路径、卡片路径。
     • 【P0 强制】最终汇报中简报下载链接和卡片下载链接二者缺一不可。若卡片链接缺失（生成失败且未修复），则需在汇报中明确说明"卡片生成暂时失败，简报已就绪"，并告知用户可在修复环境后要求补生成。

## Output
- `briefs/<company_slug>/_disambiguation.md` — 公司名消歧结果（官方全称、曾用名、品牌名）
- `briefs/<company_slug>/_db_result.json` — 数据库查询原始结果（含 found/not found + 所有字段）
- `briefs/<company_slug>/_search_log.md` — 检索溯源日志（每条检索的时间、检索词、来源 URL）
- `briefs/<company_slug>/_structured_data.md` — 结构化数据（五板块表格含来源 + 一致性检查 section；DB 命中时基本面+科创来自 DB）
- `briefs/<company_slug>/brief_<YYYY-MM-DD>.md` — 正式简报（一句话画像 + 五板块 + 快速总结 + 声明）
- `briefs/cards/<company_slug>_<YYYY-MM-DD>.png` — 分享卡片（750×1000 竖版 PNG）

## Quality Gates
- [P0-1] Step 2 缓存检查未执行就直接进入 Step 3 或 Step 4 → 引擎必须拒绝推进，退回 Step 2 补执行
- [P0-2] 五板块任一块在简报中空白且未标注「暂无公开信息」→ 打回补检索或补标注
- [P0-3] 简报中任何关键数字/日期/金额/轮次缺少「（来源：...，...）」逐条标注（DB 来源须标「科创企业数据库」），或使用「来源：A、B、C」集中罗列 → 打回逐条补上来源，或删除无来源数字
- [P0-4] 最终汇报的链接区缺少简报或卡片下载链接 → 打回补 publish_file；链接必须是 Markdown 可点击格式、各占一行，不得用纯文本路径或单行挤在一起
- [P0-5] Step 3 数据库查询失败时，未写入 `_db_result.json`（含错误信息）→ 打回补写入，确保故障可追溯
- [P0-6] Step 1 消歧发现多个候选企业时，未在 narrate 中以表格形式列出候选企业全称/城市/行业 → 打回补完整候选表格，确保用户能在对话中看清所有选项再回复
- [P1-1] 多源矛盾未在简报中双口径标注（而是择一隐匿或模糊表述）→ 判定不合规，打回补矛盾说明并重走 Step 6
- [P1-2] 某板块有效信息点 < 3 条且 Step 4 未尝试换词重搜 → 提示：先补 3 组换词检索再进入 Step 5
- [P1-3] DB 数据与公开数据存在矛盾但未在简报中双口径注明 → 打回 Step 5 补矛盾说明
