# 政策匹配 SOP (policy-match)

基于企业画像主动匹配适用政策。**对话内** 由 LLM 在识别到政策匹配意图后通过 `sop_execute('policy-match', payload={...})` 触发；**外部** 可通过 POST `/sop/policy-match` webhook 触发。

> ⚠️ **与 policy-recommend skill 的边界**:
> - `policy-recommend` skill = 用户问什么搜什么(对接外部 2048office `/policies/*` 公开 API,查询型)
> - `policy-match` SOP(本流程)= 基于当前企业的完整画像(工商+资质+三方搜索)主动匹配适用政策,**写库 + 触发小程序政策匹配页卡片推送**,推荐型
> - 用户问"政策是什么/有哪些政策" → policy-recommend
> - 用户问"我能申请什么政策 / 帮我匹配政策 / 我企业适合什么政策" → policy-match(本 SOP)

> ⚠️ **后端依赖**: 本 SOP 调用 ztagent-service-api 的三个内部接口
> - `GET {{{policy_match.api_base}}}{{{policy_match.enterprise_profile_path}}}` — 同步取企业完整画像
> - `GET {{{policy_match.api_base}}}{{{policy_match.policy_list_path}}}` — 取政策清单(~100 条全量)
> - `POST {{{policy_match.api_base}}}{{{policy_match.save_match_result_path}}}` — 写匹配结果 + 触发小程序卡片推送
>
> 三个接口都是同步的,无 rabbitmq/异步等待。所有"AI 匹配"由本地 LLM 在 SOP step 内完成,**不依赖** FastGPT 或外部 agent。

## Inputs

`sop_execute('policy-match', payload={...})` 期望的 payload:

- `enterprise_name` (string, **优先级最高**): 用户消息中明确指定的企业名(例:用户说"帮华为公司匹配下政策" → `enterprise_name="华为"`)
- `openid` (string, 必填): 当前用户 openid,作为 `agent_user_info` 透传给后端 API

**enterprise_name 兜底链**(按优先级):
1. payload 里明确传入的 `enterprise_name`(用户消息指定)
2. `memory_recall(query="user_profile_company_name")` 拿用户画像里的企业全称
3. IDENTITY.md 里渲染的 `{{enterprise.company_name}}`(provision 时注入)
4. 都没有 → narrate "请告诉我要为哪家企业匹配政策" 并以 `sop_advance status=Failed` 终止

## 工作目录

所有产物落到相对路径 `case/policy-match/<enterprise_safe>/`:

- `<enterprise_safe>` 规范化规则: 取 enterprise_name 全文,把空格 / `/` / `:` / `*` / `?` / `"` / `<` / `>` / `|` 替换成 `_`,其余字符(含中文)保留
- step 1 启动时 narrate 出 `enterprise_safe = "..."`,后续所有 step 严格复用同一字符串

## Steps

1. **取企业画像** — 同步调后端拿工商 + 资质 + 三方搜索完整画像。
   - tools: http_request, file_write, memory_recall
   - notes:
     (1) 按上文"enterprise_name 兜底链"确定 `enterprise_name`。**禁止**凭对话上下文猜测企业名 — 兜底链全部失败必须以 Failed 终止,不要硬编一个"示例企业"。
     (2) 计算 `enterprise_safe`,narrate 出 `enterprise_name="..."` 和 `enterprise_safe="..."`,后续 step 复用。
     (3) 调用接口 — **必须带 `fields` 参数**,否则全字段响应约 73 KB(含 `bocha_info`)撑爆 context:
        ```
        http_request GET {{{policy_match.api_base}}}{{{policy_match.enterprise_profile_path}}}
          ?enterprise_name=<enterprise_name>
          &agent_user_info={{openid}}
          &fields=enterprise_id,qualification_enterprise_id,enterprise_name,basic_info,qualification_info
        ```
        期望响应 `data` 含: `enterprise_id`, `qualification_enterprise_id`, `enterprise_name`, `basic_info`(工商), `qualification_info`(专利/商标/资质证书/荣誉)。
        ⚠️ **不要**拉 `bocha_info`(三方网页搜索数据 60+ KB,policy-match 业务用不上,官方注册信息 `basic_info` 才是政策对齐依据)。
     (4) 落盘到 `case/policy-match/<enterprise_safe>/profile.json` — **直接保存响应的 `data` 对象**,无需再裁剪字段(后端已按 fields 过滤好)。schema:
        ```json
        {
          "enterprise_id": <int>,
          "qualification_enterprise_id": <int>,
          "enterprise_name": "<原文>",
          "basic_info": "<原文>",
          "qualification_info": "<原文>"
        }
        ```
        **size 自检**: 落盘后期望 `profile.json` 8-15 KB(若 > 30 KB 说明 fields 参数没生效,检查调用 URL)。
     (5) **【P0】记下 `enterprise_id`**:后续 step 5 写库要用。在 narrate 中显式回报 `enterprise_id=<值>`。
     (6) **降级**: 接口 4xx/5xx → narrate "企业画像服务暂时不可用,请稍后再试",`sop_advance status=Failed`。
     (7) **数据空兜底**: 返回 `data` 为 `null` 或 `enterprise_id` 缺失 → 同上 Failed,**不要**自己捏造 enterprise_id。

2. **取候选政策清单** — 拉全量启用中的政策,仅取匹配判断所需的最小字段集,减少 context 占用。
   - tools: http_request, file_write
   - notes:
     (1) 调用 — **只取 5 个字段**,`application_condition` 和展示类字段(support_money/link 等)不在本步拉取:
        ```
        http_request GET {{{policy_match.api_base}}}{{{policy_match.policy_list_path}}}
          ?no_pagination=true
          &status=ONLINE
          &fields=id,name,policy_direction,policy_belong,application_at
        ```
        响应 `data.data[]` 每条政策含上述 5 个字段(响应体 ~6 KB)。
        ⚠️ **禁止**在 URL 里加 `order_by_fields` / `order_by_types` — 与 `no_pagination=true` 同用时后端返回 400。
     (2) 落盘到 `case/policy-match/<enterprise_safe>/candidates.json` — 保存响应的 `data.data[]` 数组:
        ```json
        {
          "policies": [
            { "id": <int>, "name": "...", "policy_direction": "...", "policy_belong": "...", "application_at": "..." }
          ]
        }
        ```
     (3) **size 自检**: 落盘后期望 `candidates.json` 3-10 KB。< 1 KB 说明数组未写入,必须重写;> 15 KB 说明 fields 参数未生效,检查 URL。
     (4) **降级**:
        - 接口 4xx → **不要**修改 fields 参数重试;narrate "政策库接口异常(4xx)",`sop_advance status=Failed`。
        - 接口 5xx 或网络错误 → narrate "政策库暂时不可用,请稍后再试",`sop_advance status=Failed`。

3. **LLM 匹配筛选(分 2 阶段,避免单次 LLM call 超 240s 红线)** — 读企业画像 + 政策清单,LLM 用粗筛 → 精筛分两次完成 Top 10 选择。
   - tools: file_read, file_write
   - notes:
     **拆分理由**: 历史观察 qwen3.6-plus/deepseek-v4-pro 一次性"100 条 → Top 10 + 完整 match_reason"的 LLM call 在 70K context 下 > 240s,撞 zeroclaw `reliable.rs` 硬编码 outer timeout。拆成两次小 LLM call(各 ≤ 90s)既绕开 timeout,也让 LLM 注意力更集中。

     ### 3a. 粗筛 — N 条 → Top 20(只输出 id 列表,不写 reason)
     - file_read `case/policy-match/<enterprise_safe>/profile.json`。**不要** file_read `candidates.json` — step 2 的 http_request 响应已在 LLM context 中,直接使用即可,重读只会浪费 context 配额。
     - 对每条候选政策,**快速**综合判断企业是否适用,判断维度:
        - **行业匹配**: 政策的 `policy_direction` / `policy_belong` 是否覆盖企业主营行业(`basic_info` 里的经营范围 / 行业分类关键词)
        - **规模匹配**: 政策若按企业规模分档(小微 / 中型 / 大型),与 `basic_info` 里的注册资本 / 参保人数对齐
        - **资质信号**: 从政策 `name` + `policy_direction` 判断是否需要"高新 / 专精特新 / 国家级资质",与 `qualification_info` 对照
        - **时效**: `application_at` 若过期(< 当前日期)且非长期有效,标记 `expired=true` 但**仍保留**(排序时排最后)
     - 选 **TOP 20**(候选不够 20 条就有几条放几条),**同时写两个文件**:
       1. `case/policy-match/<enterprise_safe>/match_coarse.json` — **schema 极简**(粗筛阶段不写 match_reason,节省 output tokens):
          ```json
          {
            "enterprise_id": <从 step 1 拿>,
            "enterprise_name": "<原始企业名>",
            "coarse_ids": [
              {"policy_summary_id": <候选 id>, "policy_summary_name": "<政策名>", "expired": false, "rank": 1}
            ]
          }
          ```
       2. `case/policy-match/<enterprise_safe>/candidates_shortlisted.json` — 从 context 中的 step 2 响应提取这 TOP 20 条的完整 5 字段数据,**schema 与 candidates.json 相同**:
          ```json
          {
            "policies": [
              { "id": <int>, "name": "...", "policy_direction": "...", "policy_belong": "...", "application_at": "..." }
            ]
          }
          ```
          **为什么写这个**: step 3b 精筛时只需这 20 条的详情,读这个文件比读全量 candidates.json 小得多,节省 context。
     - **【P0 反幻觉】**: `policy_summary_id` 必须来自 step 2 http_request 响应中真实出现的 `id`,**禁止**凭训练记忆生造。
     - **size 自检**: `match_coarse.json` 期望 2-5 KB,`coarse_ids[]` 长度 ≤ 20;> 8 KB 说明 LLM 写了多余字段(例如把 reason 也写进来了),重写。`candidates_shortlisted.json` 期望 ≤ 4 KB;> 8 KB 说明把全量写进去了,按 coarse_ids 重新截取。
     - **零匹配兜底**: 全量政策都不适用 → `coarse_ids` 写空数组,`candidates_shortlisted.json` 写空 `policies[]`,跳过 3b 直接进 step 4(step 4 会处理空列表)。

     ### 3b. 精筛 — Top 20 → Top 10(带 match_reason)
     - file_read `match_coarse.json` 和 `candidates_shortlisted.json`。(**不要** file_read `candidates.json` — shortlisted 已含这 20 条的完整字段,全量文件只会带入多余 context。)
     - 从 `coarse_ids[]` 的 20 个 id 中,基于 `candidates_shortlisted.json` 里这 20 条对应的详情,精选 **TOP 10**,为每条写 **15 字内** match_reason(精简,不要"该政策"等冗余开头)。
     - 输出写入 `case/policy-match/<enterprise_safe>/match.json`,schema:
        ```json
        {
          "enterprise_id": <从 match_coarse.json 拿>,
          "enterprise_name": "<原始企业名>",
          "matched_policies": [
            {
              "policy_summary_id": <候选 id>,
              "policy_summary_name": "<政策名>",
              "match_reason": "<≤ 15 字>",
              "expired": false,
              "rank": 1
            }
          ]
        }
        ```
     - **【P0 反幻觉】**: `policy_summary_id` 必须从 3a 的 `coarse_ids[]` 选,不要重新引入 candidates 里被 3a 淘汰的 id。
     - **零匹配处理**: 3a 输出空 `coarse_ids` 时,3b 直接写空 `matched_policies[]`,narrate "企业画像信息不足以匹配政策,建议在小程序补全企业资质后重试",推进到 step 4。

4. **条件分析** — 对每条匹配政策的申报条件做逐项拆解,标注企业满足情况。
   - tools: file_read, http_request, file_write
   - notes:
     (1) file_read `profile.json` 和 `match.json`。
     (2) **先拉 application_condition** — 调一次接口获取全量政策的申请条件:
        ```
        http_request GET {{{policy_match.api_base}}}{{{policy_match.policy_list_path}}}
          ?no_pagination=true
          &status=ONLINE
          &fields=id,application_condition
        ```
        响应约 39 KB。从结果里按 `matched_policies[]` 中的 id 找对应的 `application_condition`。
     (3) 对 `matched_policies[]` 里每条政策,**从(2)的响应中提取对应 id 的 `application_condition`**,把整段条件文本拆成 3-8 个**离散条件项**(例:"申报主体需为高新技术企业"、"上年度营收 ≥ 500 万"、"研发人员占比 ≥ 10%")。
     (4) 对每项条件,基于 `profile.json` 里的信息判断 `match_status`:
        - `"yes"`: 企业明确满足(例:profile 里有"高新技术企业"证书)
        - `"no"`: 企业明确不满足(例:营收要求 500 万但企业上年度营收 100 万)
        - `"partial"`: 部分满足或临界(例:营收要求 500 万,企业 480 万)
        - `"unknown"`: 画像缺失对应信息(例:政策要求研发人员占比,画像里没有该字段) — **不要**编造数字,标 unknown
     (4) 输出写入 `case/policy-match/<enterprise_safe>/condition.json`,schema:
        ```json
        {
          "enterprise_id": <从 match.json>,
          "conditions_by_policy": [
            {
              "policy_summary_id": <id>,
              "policy_summary_name": "<政策名>",
              "conditions": [
                {
                  "requirement": "<条件原文或精炼描述>",
                  "enterprise_profile": "<企业对应字段实际值,unknown 则写 'N/A'>",
                  "match_status": "yes|no|partial|unknown"
                }
              ]
            }
          ]
        }
        ```
     (5) **`requirement` 字段不要漏写**:写库时后端按这个字段建 `EnterprisePolicySummaryCondition` 行,空字符串会污染前端展示。
     (6) **【P0 合并输出 — 在本步完成,不要留到 step 5】** 在 `condition.json` 落盘之后,立刻写入 `case/policy-match/<enterprise_safe>/request_body.json`。构造方式:
        - 遍历 `match.json` 里的每个 `matched_policies` 元素(共 N 条);
        - 对每条,在 `condition.json` 的 `conditions_by_policy[]` 里找 `policy_summary_id` 相同的那条,取出它的 `conditions[]`;
        - 把 `conditions[]` 注入到该 `matched_policies` 元素中,组成完整条目;
        - 最终 schema:
          ```json
          {
            "enterprise_id": <从 match.json>,
            "agent_user_info": "{{openid}}",
            "matched_policies": [
              {
                "policy_summary_id": <id>,
                "policy_summary_name": "<政策名>",
                "match_reason": "<≤15字>",
                "rank": <int>,
                "expired": false,
                "conditions": [
                  {"requirement": "...", "enterprise_profile": "...", "match_status": "yes|no|partial|unknown"}
                ]
              }
            ]
          }
          ```
        - **size 自检**: `request_body.json` 期望 5-40 KB;`matched_policies[]` 长度必须等于 `match.json` 里 `matched_policies[]` 长度(0 或 N 条),**不能变成空数组**。若发现 `matched_policies` 变成 `[]` 说明 join 失败,必须重试合并。
        - **`agent_user_info` 字段**: 直接把 `{{openid}}` 字面量写入文件即可(字符串)。

5. **写库 + 触发小程序卡片推送** — 读 step 4 已合并好的请求体,一次性原子写入后端,后端触发小程序政策匹配页卡片推送。
   - tools: file_read, http_request
   - notes:
     (1) file_read `request_body.json`(step 4 已完成 match.json + condition.json 的合并,本步**只读这一个文件**,不要再次 file_read match.json / condition.json)。
     (2) 调用(body 直接用 `request_body.json` 的完整内容,不要手动重构字段):
        ```
        http_request POST {{{policy_match.api_base}}}{{{policy_match.save_match_result_path}}}
          Content-Type: application/json
          body: <request_body.json 的完整内容>
        ```
        ⚠️ **禁止**在本步重新构造 `matched_policies` 数组 — step 4 已经做好了合并,直接发送即可。重新构造极易变成空数组。
     (3) 期望后端语义(本 SOP 假定后端契约):**原子替换** — 先按 `enterprise_id` 清空旧 `EnterprisePolicySummary` + `EnterprisePolicySummaryCondition`,再写入新数据,**同时触发** Redis weapp 推送通道(小程序政策匹配页卡片由前端订阅 SSE/轮询拿到刷新)。
     (4) **降级**:
        - 接口 4xx → 大概率是 schema 不匹配或 enterprise_id 不存在,narrate 错误内容,**仍继续 step 6** 把已算出的结果在对话里展示给用户(不至于让用户白等),但提醒"小程序政策匹配页可能未同步"
        - 接口 5xx → narrate "结果保存暂时失败",继续 step 6 展示对话回复,但提醒"请稍后到小程序政策匹配页刷新"
     (5) **空匹配跳过**: `request_body.json` 里 `matched_policies` 为空数组时,**不调** POST(避免清空用户已有匹配数据),直接推进到 step 6。

6. **对话回复概要 + 引导留资** — 在飞书/小程序对话内输出可读的概要,引导用户到小程序看详情 + 主动建议留资。
   - tools: file_read
   - notes:
     (1) file_read `profile.json` + `match.json` + `condition.json`,组织对话回复(profile.json 用来取 `qualification_enterprise_id`,作为小程序深链 url 的 `id` 参数)。
     (2) **回复格式**(严格):
        ```
        已为 **<enterprise_name>** 匹配 <N> 条适用政策(完整条件分析已同步到小程序政策匹配页):

        **TOP 3 优先级**:
        (1) **<政策名 1>** — <match_reason>
        (2) **<政策名 2>** — <match_reason>
        (3) **<政策名 3>** — <match_reason>

        其余 <N-3> 条 + 每条详细条件评估 → [小程序政策匹配页]({{{policy_match.mini_program_detail_path_template}}})

        ⚠️ 以上为基于当前画像的初步匹配,具体可行性以政策原文和评审结论为准。

        想让政策顾问帮您做一次资质评估/申报路径规划吗?留个手机号或微信号即可。
        ```
     (3) 链接里的 `{qualification_enterprise_id}` 占位符,**手动替换**成 step 1 `profile.json` 里读到的 `qualification_enterprise_id` 数字(注意:**不是 `enterprise_id`** — `enterprise_id` 是 step 5 写库的主键,小程序深链按资质企业表的主键 `qualification_enterprise_id` 路由)。
     (4) **N≤3 的边界**: 匹配数小于 3 条时,"TOP 3 优先级"段直接列实际条数;`其余 N-3 条` 段省略不写。
     (5) **零匹配**: `matched_policies` 为空时,回复:
        ```
        基于当前掌握的 <enterprise_name> 画像信息,暂未匹配到强相关的政策。这通常是因为企业资质/营收/研发等关键字段在画像里尚未完整。建议:
        (1) 在小程序补全企业资质(高新/专精特新/研发投入等)
        (2) 补全后再让我重新匹配一次

        或者您希望政策顾问主动联系您,基于详细沟通后给出推荐?留个手机号或微信号即可。
        ```
     (6) **【P0 输出契约】**:回复**不要**夹杂"我调了 API"、"step 5 失败"、"json 解析异常"这类内部日志。后端写库失败时只说"小程序政策匹配页可能稍后才同步,请刷新"。
     (7) **不要**在本步主动调 `memory_store` 或飞书 webhook 留资 — 留资发生在用户**后续回复了联系方式**之后,由 IDENTITY.md 里的留资规则触发(本 SOP 至此已 Completed)。

## SOP 结束后的留资跟进(交由 IDENTITY.md 规则处理)

本 SOP 跑完后,如果用户后续消息里提供了**手机号/微信号/邮箱**,由 IDENTITY.md 里的"政策匹配留资"规则触发(参考 policy-recommend skill 的同款逻辑,memory category 改用 `policy-match-lead` 区分):

1. `memory_store(key="policy-match-lead-<联系方式末四位>-<日期>", content="<合并的诉求+联系方式+企业名+匹配出的政策方向>", category="policy-match-lead")`
2. 调飞书 webhook(IDENTITY 里 `{{lead.webhook_url}}` 已配置时):POST {"msg_type":"text","content":{"text":"📋 政策匹配留资\n企业:<name>\n联系人:<称呼>\n联系方式:<...>\n匹配政策:<TOP 3 政策名>\n诉求:<评估资质/申报指导/...>\n时间戳:<YYYY-MM-DD HH:MM>"}}
3. 给用户回:"好的,已经登记,政策顾问会在 1 个工作日内主动联系您。"

这部分写在 IDENTITY.md 的"政策匹配留资"段,不放在本 SOP 内 —— SOP 必须同步完成,无法等待用户后续回话。
