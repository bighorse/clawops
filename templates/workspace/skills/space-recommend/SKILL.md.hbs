# SKILL.md — space-recommend

用户想【租/找 办公室、工位、园区、众创空间、孵化器、联合办公、厂房、产业用地、选址落地】时触发。调用 2048office space-v2 API 查询推荐怀柔科学城内的空间资源，给出详情页链接。

## 元数据

- **名称**: space-recommend
- **描述**: 对接 2048office 平台 `/space-v2/*` API，查询并推荐怀柔科学城内的**园区（办公楼宇）/ 众创空间 / 厂房 / 产业用地**，按用户需求做候选筛选并给出小程序详情页链接
- **触发词**: 用户消息中含**找空间 / 选址 / 落地**意图 → **必触发**：
  - "找园区 / 找空间 / 园区推荐 / 有什么园区 / 落地园区 / 有没有办公室"
  - "租办公室 / 租办公 / 要租 / 想租 / 租个办公室 / 办公室出租 / 办公空间"
  - "众创空间 / 联合办公 / 共享办公 / 创业空间 / 孵化器 / 加速器"
  - "厂房 / 标准厂房 / 独栋 / 研发用房"
  - "独立办公室 / 整层办公 / 写字楼"
  - "工位 / 工位出租 / 共享工位"
  - "产业用地 / 工业用地 / 拿地"
  - 用户提到具体园区名（如"长城海纳 / 怀柔科学城城市客厅 / XX 加速器"）→ **立即触发**
- **⛔ 不触发（走 general-information）**：用户问"大科学装置 / 交叉研究平台 / 科教基础设施 / HEPS / 综合极端条件 / 生物医学成像 是什么 / 有哪些 / 怎么联系"这类**科研设施介绍性查询**——这些不是可租赁的园区空间，走 `general-information` 而不是本 skill

---

## ⚠️ 核心原则

- **查询类**（浏览空间 / 看详情）：自动执行，无需确认
- **写操作**（新增 / 修改 / 删除 / 状态变更 / 需求提交）：**全部禁止调用** — 引导用户到详情页或留联系方式
- **不调 API 不答空间**：**禁止**凭训练记忆给园区名/地址/价格/面积 — 这些是平台运营数据，写出来 100% 是编造
- **HTML 字段**：`description`、`profile`、`investment_policy` 等长字段可能含 HTML，展示时**剥标签**，不要原样输出 HTML

---

## API 基础信息

- Base URL: `{{{space.api_base}}}`
- 鉴权: 公开查询端点免 token，直接调
- 响应包络: `{"request_id":..., "message":"", "data":{"page_index":1, "page_size":N, "data_count":M, "data":[...]}}` — 列表型真数据在 `.data.data[]`，详情型在 `.data` 里

### 主要端点（本 skill 只用这些）

| 用途 | 端点 | 方法 |
|------|------|------|
| **园区搜索（主路径）** | `{{{space.api_base}}}/parks/new_query_list` | POST，body JSON |
| **园区详情** | `{{{space.api_base}}}/parks/item?id={park_id}` | GET |
| **众创空间列表** | `{{{space.api_base}}}/space_show/maker_space/list?status=2&page_index=1&page_size=10` | GET |
| **众创空间详情** | `{{{space.api_base}}}/space_show/maker_space/item?maker_space_id={id}` | GET |
| **独立办公室列表** | `{{{space.api_base}}}/space_show/office/list?status=2&page_index=1&page_size=30` | GET |
| **独立办公室详情** | `{{{space.api_base}}}/space_show/office/item?office_id={id}` | GET |
| **基础字典（解析 ID）** | `{{{space.api_base}}}/data/base/list?data_type={N}` | GET |
| 厂房列表 | `{{{space.api_base}}}/space_show/workshop/list?status=2&page_index=1` | GET |
| 厂房详情 | `{{{space.api_base}}}/space_show/workshop/item?workshop_id={id}` | GET |
| 产业用地列表 | `{{{space.api_base}}}/space_show/industrial_land/list?status=2&page_index=1` | GET |

**禁用端点**（LLM 不要调）：
- 任何 `/parks/back/*`、`/parks/building/back/*`、`/parks/floor/back/*`、`/parks/room/back/*` — 后台管理
- 旧端点 `POST /parks/query_list` — **已废弃，不要用**，用 `new_query_list`
- 任何 POST/PUT/DELETE（除了 `POST /parks/new_query_list`）
- `/incubatorpark/*`、`/demand/*` — 入驻审核和需求提交

---

## 基础字典（data/base/list）— 重要

`GET {{{space.api_base}}}/data/base/list?data_type={N}` 返回枚举字典，用于把 ID 翻译成可读名称：

| data_type | 含义 | 用于解析 |
|-----------|------|---------|
| 1 | 服务配套 | 众创空间 `service_matching` 字段 |
| 3 | 所属行业 | 筛选参考 |
| 4 | 招商方向 | 众创空间 `investment_direction` 字段（如为 ID 时）|
| 5 | 产业特色 | 园区 `industrial_characteristics` 字段 |
| 6 | 土地规划用途 | 产业用地 |

响应格式：`.data.data[]` 每条含 `{id, data_name, data_type, ...}`

**使用规则**：
1. 展示众创空间列表/详情前，**必须先调** `data/base/list?data_type=1` 拿服务配套字典
2. 拿到字典后，把 `service_matching`（如 `"1,3,4,5,239"`）按逗号分隔 → 转 int → 在字典中查 `id` → 取 `data_name` → 拼中文列表展示
3. `investment_direction` 若也是数字串，同理用 `data_type=4` 字典解析
4. 字典调用可与主列表查询**并行**（不需要先等字典再查列表）

---

## POST /parks/new_query_list — 园区搜索参数

```json
{
  "keyword": "<关键词，可空>",
  "page_index": 1,
  "page_size": 10
}
```

- `keyword`：支持园区名称模糊搜索（如 `"长城海纳"`）
- 响应里每条园区的主键是 **`park_id`**（不是 `id`），详情页链接和 `parks/item` 都用 `park_id`

**园区列表返回的关键字段**：

| 字段 | 含义 |
|------|------|
| `park_id` | 主键（详情查询和链接都用这个，**不是 `id`**）|
| `park_name` | 园区名称 |
| `park_address` | 地址 |
| `provider_name` | 运营方 |
| `park_images` | 图片（逗号分隔，第一张为封面）|
| `average_price` | 参考均价 |
| `gross_floor_area` / `area` | 建筑面积 / 占地面积 |
| `investment_direction` | 招商方向（**对象数组** `[{id, name}]`，直接取 `.name`）|

**园区详情（`parks/item`）额外字段**：

| 字段 | 含义 |
|------|------|
| `description` | 园区简介（**可能含 HTML，必须剥标签**）|
| `space_appearance` | 空间形态（数组，枚举：5 物流仓储 / 6 科技园区 / 7 商圈）|
| `plot_ratio` | 容积率 |
| `pure_price` | 房租裸价（元/㎡/天）|
| `property_fee` | 物业及空调费 |
| `principal_name` / `contact_number` | 联系人 / 电话 |

---

## 众创空间字段说明

**列表** `GET /space_show/maker_space/list?status=2&page_index=1&page_size=10`（名称搜索加 `&name=<keyword>`）：

| 字段 | 含义 |
|------|------|
| `id` | 主键（详情查询用，传给 `maker_space_id` 参数）|
| `name` | 众创空间名称 |
| `address` | 地址 |
| `area` | 总面积（㎡）|
| `space_images` | 图片（逗号分隔）|
| `service_matching` | 服务配套（**逗号分隔数字 ID 字符串**，必须用字典解析）|
| `investment_direction` | 招商方向（若为数字串则需字典解析）|
| `station_min_unit_price` | 工位最低价（元/工位/月）|
| `office_min_unit_price` | 办公室最低价（元/间/月）|

**详情** `GET /space_show/maker_space/item?maker_space_id={id}` 额外字段：

| 字段 | 含义 |
|------|------|
| `profile` | 简介（**可能含 HTML，剥标签**）|
| `investment_policy` | 招商政策（**可能含 HTML，剥标签**）|
| `contact` / `contact_tel` | 联系人 / 电话 |

---

## 执行流程：5 步自主选址顾问

### Step 1：理解需求，锁定资源类型

| 用户诉求 | 资源类型 | 主入口 |
|---------|---------|--------|
| **独立办公室 / 办公间 / 租房间 / 指定面积（如 100–1000㎡）** | **独立办公室** | `GET /space_show/office/list?status=2&page_size=30` — 拉全量后**端侧按 `floor_area` 过滤**面积范围 |
| 众创空间 / 联合办公 / 孵化器 / 加速器 / 工位 / 共享工位 | **众创空间** | `GET /space_show/maker_space/list?status=2` |
| 园区 / 楼宇 / 选址 / 落地 / 具体园区名 | **园区** | `POST /parks/new_query_list` |
| 厂房 / 研发用房 | 厂房 | `GET /space_show/workshop/list?status=2` |
| 产业用地 | 产业用地 | `GET /space_show/industrial_land/list?status=2` |
| 不确定（"找个地方落地"等泛问题）| 同时查园区 + 众创空间 | 两个 API 都调 |

> ⚠️ **用户提到具体名称时**（如"长城海纳"、"城市客厅"）：`keyword` 直接传用户说的词，**不要缩减或扩展**。如果搜不到，再试更短的词（见 Step 2 宽搜规则）。

### Step 2：搜索（自动执行）

#### 2a 园区搜索

```
http_request POST {{{space.api_base}}}/parks/new_query_list
Content-Type: application/json
body: {"keyword": "<用户关键词，无则空字符串>", "page_index": 1, "page_size": 10}
```

如用户说的是某特定园区名但 `data_count=0`，**必须宽搜**：
1. 截取前 2–4 个汉字重试（如 "长城海纳硬科技加速器" → "长城海纳"）
2. 若仍无结果，再试不带 keyword 的全量列表（`keyword: ""`）后端侧模糊匹配名称
3. **禁止在只做一次搜索后就断言"数据库里没有"**

#### 2b 众创空间搜索

```
http_request GET {{{space.api_base}}}/space_show/maker_space/list?status=2&page_index=1&page_size=10
（有关键词加 &name=<keyword>）
```

⚠️ `status=2` 必须带，否则会包含已下线的空间。

#### 2c 字典并行获取（任何时候展示众创空间都需要）

与主搜索同时发起（不要串行等待）：

```
http_request GET {{{space.api_base}}}/data/base/list?data_type=1
```

拿到字典 `dict1`（服务配套）。若 `investment_direction` 也是数字串，额外调 `data_type=4`。

### Step 3：可行性校验（过滤候选）

**独立办公室**：API 不支持服务端面积过滤，必须**端侧过滤**：
1. 对返回的 `.data.data[]`，保留 `floor_area >= 用户下限` 且 `floor_area <= 用户上限` 的房间
2. 若无精确匹配，放宽到"最接近的 5 间"，告知用户"当前平台面积最大的办公室是 XXX ㎡"
3. `direction` 字段格式为 `,4,5,`（头尾有逗号），需去除空字符串后按逗号分隔为 ID 数组，用 `data_type=4` 字典解析

**园区**：`new_query_list` 后端已过滤状态，无需端侧 status 过滤。过滤条件：
1. `park_name` 与用户诉求真实相关
2. 无价格信息（`average_price=0` 或 null）写"面议"，不编造数字

**众创空间**：已带 `status=2` 参数，端侧仅做相关性判断。过滤：
1. `name` 与用户诉求真实相关
2. 独立办公室/工位下钻时，父级众创空间必须已通过上面的 status=2 过滤

留 TOP 5。若过滤后 < 2 条，告诉用户"暂无强匹配，以下是接近的"。

### Step 4：展示推荐清单

⚠️ **铁律**：列出的每一个字段、数字、词，都必须来自 API 响应原文。不补全名字、不编造地标、不合并字段做语义包装、不猜招商方向。

**独立办公室**模板：

```
**N. <office_name>** — <maker_space_name>
- 建筑面积：<floor_area> ㎡（使用面积：<usable_area> ㎡）
- 价格：<office_price 元/月；0/null 时写"面议">
- 可容纳：<max_capacity> 人（0/null 时省略此行）
- [查看所在空间详情](把 maker_space_id 代入众创空间详情路径模板)（进入后点击「联系预约」，注明房间号 <office_name>）
```

众创空间详情路径模板：`{{{space.maker_space_detail_path_template}}}` —— 把 `{id}` 换成 `maker_space_id`。

⚠️ **独立办公室没有独立详情页**——小程序的众创空间详情页只展示部分房间，用户可能看不到具体房间号。因此每条办公室回复里**必须把房间号（`office_name`）在正文中写清楚**，引导用户在详情页联系时主动说明房间号。

**园区**模板：

```
**N. <park_name>** — <provider_name>
- 地址：<park_address>
- 占地面积：<area> 亩（0/null 时省略此行）
- 价格：<average_price 元/㎡/月，0/null 时写"面议">
- 招商方向：<investment_direction 数组中每个 .name，逗号分隔；空则省略此行>
- 简介：<description 剥 HTML 截 60 字内，带省略号；为空则省略>
- [查看园区详情](把 park_id 代入路径模板)
```

园区详情路径模板：`{{{space.park_detail_path_template}}}` —— 把 `{id}` 换成 API 返回的 `park_id`。例如 park_id=5 → 链接目标 `/pages/space/park?id=5`。

**众创空间**模板：

```
**N. <name>** — <enterprise_name 或 provider_name>
- 地址：<address>
- 总面积：<area> ㎡（0/null 时省略）
- 工位价格：<station_min_unit_price 元/工位/月；null 时省略>
- 办公室价格：<office_min_unit_price 元/间/月；null 时省略>
- 服务配套：<用 data/base/list?data_type=1 字典解析后的中文名列表，挑前 3–5 个；若解析结果为空或全是数字则整行省略>
- [查看众创空间详情](把 maker_space_id 代入路径模板)
```

众创空间详情路径模板：`{{{space.maker_space_detail_path_template}}}` —— 把 `{id}` 换成 API 返回的 `maker_space_id`。例如 maker_space_id=9 → 链接目标 `/pages/space/makerDetail?id=9`。

⚠️ **服务配套 / 招商方向严禁展示原始数字 ID**：
- `service_matching = "1,3,4,239"` → 必须解析为 `"工商注册、财税服务、法律顾问、..."` 后再展示
- 若字典请求失败或解析结果全为空，**整行省略**（写"服务配套:1,3,4" 对用户毫无意义，是生产事故）

末尾固定加：

```
要看哪个空间的详情、对比，或留下需求让顾问联系你？告诉我即可。
```

### Step 5：用户追问详情（按需执行）

**园区详情**：
```
http_request GET {{{space.api_base}}}/parks/item?id=<park_id>
```
整理展示：园区简介（剥 HTML）、园区参数（面积/价格/空间形态）、联系人/电话、详情页链接。

**众创空间详情**：
```
http_request GET {{{space.api_base}}}/space_show/maker_space/item?maker_space_id=<id>
```
同时调 `data/base/list?data_type=1` 解析 `service_matching`。整理展示：简介、政策介绍、参数（面积/工位价格/办公室价格/服务配套）、联系人/电话。

**对比场景**：用户指定多个园区 → 分别调 `parks/item` 拿完整数据，以**表格**形式展示关键字段。

---

## 降级策略

| 情况 | 处理 |
|------|------|
| API 401/403 | 告诉用户"空间库暂时不可用"，**不**编造空间 |
| 搜无结果（`data_count=0`）| 先宽搜（截短关键词）再试；仍无 → 告诉用户"暂无符合条件的空间，可告诉我具体面积/行业/预算再试" |
| **用户说"X 有空间"但搜不到** | **必须执行三步宽搜再下结论**：①原始 keyword → ②截短 2–4 字 → ③空 keyword 全量后端侧名称匹配。三步后仍无 → 告知"系统目前未录入该空间的公开信息，建议直接联系运营方了解"。**严禁说"数据库/DB 没有"** — 你查的是公开 API，后台可能有数据只是未上线，说"DB 没有"是错误断言 |
| `data/base/list` 失败 | `service_matching` 字段整行省略，不展示原始 ID |
| 网络超时 | 告诉用户"空间平台暂时取不到，稍后再试"，**不**编造 |
| 用户问"帮我预订/锁定/谈价" | "我只做推荐和信息对比，锁定/谈价请到详情页留联系方式或直接联系运营方" |

---

## 自检（回答前必过）

1. **园区 ID**：我用的是 `park_id`（不是 `id`）吗？`new_query_list` 响应里主键是 `park_id`，错用 `id` 会导致详情页 404。
2. **service_matching 已解析**：我展示的众创空间服务配套是**中文名**不是数字？如果是数字，**回去解析或整行删除**。
3. **众创空间带 status=2**：我的 maker_space list 请求带了 `status=2` 参数吗？没带就可能展示下线空间。
4. **没有数字 ID 裸露**：所有展示给用户的字段中，没有 `1,3,4,5,239` 这类纯数字枚举值吧？有就回去解析或删行。
5. **字段逐一对 API 原文**：每条空间的每个字段（名字/地址/面积/价格/联系人）都能在 API 响应里找到原值？找不到就是编造，**删除**。
6. **名字字面对比**：我写出的名字，跟 API `park_name`/`name` 字段**一字不差**？加了后缀/省略了字就改回来。
7. **HTML 已剥**：`description`/`profile`/`investment_policy` 展示前有没有剥 HTML 标签？有标签残留就回去处理。
8. **链接正确**：园区用 `park_detail_path_template` + `park_id`；众创空间用 `maker_space_detail_path_template` + `id`（maker_space_id）。
9. **写操作禁止**：有没有调除 `POST /parks/new_query_list` 以外的 POST/PUT/DELETE？有就删掉。

任一项答 No，**回去重新执行**。

---

## 用户留资（留下联系方式 → 通知运营群）

空间推荐后用户表达"想看房 / 想锁定 / 想要报价 / 让顾问联系"时，主动建议留联系方式。当用户给出手机号/微信号/邮箱，**必须**做以下两步：

**第一步：本地存档** — 调 `memory_store`：
- `key`: `space-lead-<联系方式末四位或微信号>-<日期 YYYYMMDD>`
- `content`: 称呼 + 联系方式 + 意向空间 + 诉求摘要（面积/预算/行业）
- `category`: `"lead"`

{{#if lead.enabled}}
**第二步：通知运营群** — 调 `http_request` POST 飞书 webhook：

```
url: {{{lead.webhook_url}}}
method: POST
headers: {"Content-Type": "application/json"}
body: {
  "msg_type": "text",
  "content": {
    "text": "🏢 空间留资\n意向类型:[园区/众创空间/厂房/其他]\n意向标的:[具体名称]\n联系人:[姓名/称呼]\n联系方式:[手机/微信/邮箱]\n用户企业:[memory 里 user_profile_company_name，没有写 未填]\n行业:[memory 里 user_profile_industry，没有写 未填]\n核心诉求:[面积/预算/行业方向等]\n时间戳:[YYYY-MM-DD HH:MM]"
  }
}
```

webhook 失败不重试，不告诉用户失败。
{{else}}
**第二步当前未启用** — 只完成第一步 `memory_store` 即可。
{{/if}}

给用户的回复：`好的 [称呼]，已经登记，运营顾问会在 1 个工作日内主动联系你。`

⚠️ 不要说"已发送到飞书群"，违反 SOUL 输出契约。

---

## 与其他技能的边界

- 用户问**入驻孵化器的资质审核流程** → 告知"请到小程序内提交资质材料，客服会联系"
- 用户问**空间相关代办服务**（装修代办/入驻协办）→ 走 `commodity-recommend`
- 用户问**政策（产业补贴、租金减免）** → 走 `policy-recommend`，本 skill 仅引用空间自身招商政策字段
- 用户问**活动（园区组织的对接会/培训）** → 走 `activity-recommend`
