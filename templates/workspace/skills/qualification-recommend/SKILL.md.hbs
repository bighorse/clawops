{{#if qualification.enabled}}
# SKILL.md — qualification-recommend

用户想【查企业资质、问能申请/申报什么资质、资质到期提醒、获得资质推荐建议】时触发。调用资质服务返回企业资质详情页链接，供用户在小程序内查看现状与申报建议。

## 元数据

- **名称**: qualification-recommend
- **描述**: 对接 2048office 平台资质服务，通过后端微服务处理企业资质相关数据，返回企业资质详情页链接，让用户在小程序内查看资质现状与申报建议
- **触发词**: 用户消息中含**企业资质查询**意图 → **必触发**:
  - "查资质 / 帮我查资质 / 看一下资质 / 资质怎么样"
  - "我有哪些资质 / 我们公司有什么资质 / 资质清单"
  - "资质到期 / 资质快到期了 / 证书到期"
  - "能申请什么资质 / 哪些资质可以申报 / 推荐资质"
  - "高新 / 专精特新 / 双软 / 知识产权 / CMMI"（加"资质""认定""申请"等词时）
  - "资质评估 / 资质分析 / 看看我企业的资质状态"

> ⚠️ **与 `qualification-check` SOP 的边界**：本 skill 调用 `/resolve_enterprise_qualification` 一键触发后端处理，速度快，适合用户快速查看资质详情页；`qualification-check` SOP 是深度多步骤资质分析（含天眼查同步 + LLM 分析 + 申报建议）。两者互不替代 —— **本 skill 先跑，需要更深度分析时再走 SOP**。

---

## ⚠️ 核心原则

1. **不调 API 不答资质细节**：用户问"我有哪些资质 / 资质状态怎样"，**必须先调 http_request**；**禁止**凭训练记忆给资质名称或到期时间 —— 这些是实时数据。
2. **一次调用返回详情页链接**：本 skill 只负责调接口、拿链接、展示链接，具体资质数据在小程序详情页里，**不要**假装自己能看到详情页内容。
3. **企业名必须准确**：企业全称有一个字错误后端就会返回 404，必须从可靠来源取（见下方 Step 1）。
4. **非 200 响应**：直接把 `message` 字段内容告诉用户，建议核实企业名称是否正确。

---

## API 基础信息

- **接口地址**：`{{{qualification.api_base}}}/agent/resolve_enterprise_qualification`
- **请求方式**：POST
- **鉴权**：无需 token
- **用户标识**（已渲染，直接用）：
  - `agent_user_info`：`{{openid}}`
  - `mobile`（小程序用户需传）：`{{#if phone}}{{phone}}{{else}}（用户未授权手机号）{{/if}}`

---

## 执行流程：3 步

### Step 1：确认企业名

按以下优先级取企业全称：
1. 当前消息中用户明确说的企业名
2. `memory_recall(query="user profile company")` 拿 `user_profile_company_name`
3. 对话历史里最近提到的企业名

**三者都没有时**：只问一句"请提供企业的完整全称"，用户回复后立刻执行 Step 2，**不要**再追问。

### Step 2：调接口

根据 `agent_user_info` 格式选请求体：

**情况 A — `agent_user_info` 以 `uin:` 开头（微信企业号用户）**：

```
http_request POST {{{qualification.api_base}}}/agent/resolve_enterprise_qualification
Content-Type: application/json

{
  "agent_user_info": "{{openid}}",
  "enterprise_name": "<Step 1 取到的企业全称>"
}
```

**情况 B — `agent_user_info` 不以 `uin:` 开头（小程序用户）**：

```
http_request POST {{{qualification.api_base}}}/agent/resolve_enterprise_qualification
Content-Type: application/json

{
  "agent_user_info": "{{openid}}",
  "enterprise_name": "<Step 1 取到的企业全称>"{{#if phone}},
  "mobile": "{{phone}}"{{/if}}
}
```

### Step 3：处理响应

**HTTP 200 — 成功**：

从 `data.wechat_page_url` 取详情页路径，按以下格式回复：

```
已为 **<企业名>** 触发资质数据处理，点击查看详情：

[查看企业资质详情](把 wechat_page_url 代入路径模板)
```

⚠️ **链接构造是内部操作，绝对不要把处理过程输出给用户**：在你脑子里完成，直接写入最终回复，不要 narrate"第1步去掉html、第2步加斜杠"。规则：把 `data.wechat_page_url`（如 `pages/qualification/index.html?id=44`）去掉 `.html`、补 `/` 前缀，得到 `/pages/qualification/index?id=44`，直接用这个值替换 `[查看企业资质详情](...)` 括号里的内容。

**HTTP 非 200 — 失败**：

直接告知用户：
> 资质查询未能完成：「`<data.message 字段内容>`」。请核实企业全称是否完整准确，如有问题告诉我，我重新查询。

---

## 留资（用户想要顾问跟进）

用户在资质查询后表达"想要更深度对接 / 让顾问帮我 / 咨询具体怎么申报"时，**主动建议**留联系方式。当用户给出手机号 / 微信号 / 邮箱，**必须**执行以下两步：

### 触发条件（任一发生即进入留资流程）

- 用户问"我能不能申请 X 资质 / 够不够格 / 成功率怎么样"（超出 API 数据范围）
- 用户想让顾问评估或代办申报
- 用户主动说"让顾问联系我"

→ 回复：**"我帮你登记一下，资质顾问会主动联系你。麻烦留一个手机号或微信号 + 你的称呼 + 想了解的资质方向。"**

### 用户给了联系方式后 — 两步

**第一步：本地存档** — 调 `memory_store`：
- `key`：`qualification-lead-<联系方式末四位或微信号>-<日期 YYYYMMDD>`
- `content`：称呼 + 联系方式 + 企业名 + 关注资质方向 + 诉求摘要
- `category`：`"lead"`

{{#if lead.enabled}}
**第二步：实时通知运营群** — 调 `http_request` POST 飞书 webhook：

```
url: {{{lead.webhook_url}}}
method: POST
headers: {"Content-Type": "application/json"}
body: {
  "msg_type": "text",
  "content": {
    "text": "📋 资质留资\n联系人:[姓名 / 称呼]\n联系方式:[手机号 / 微信号]\n用户企业:[memory 里的 user_profile_company_name，无则写 未填]\n关注资质:[高新 / 专精特新 / 双软 / 其他，用户提到的]\n核心诉求:[资质评估 / 申报指导 / 代办 / 其他]\n时间戳:[YYYY-MM-DD HH:MM]"
  }
}
```

调用规则：`msg_type` 必须 `"text"`；占位符全部替换真实内容；失败不重试，不告诉用户。
{{else}}
**第二步当前未启用** — 飞书 webhook 未配置，只完成第一步即可。
{{/if}}

### 给用户的回复

> 好的 [称呼]，已经登记，资质顾问会在 1 个工作日内联系你。

⚠️ **不要**说"已发通知 / 已推送给运维"。

---

## 自检（回答前必过）

1. **企业名**是来自可靠来源（用户明说 / memory_recall / 对话历史）而不是我猜的？
2. **request body** 里的 `agent_user_info` 是 `{{openid}}` 这个实际值，不是占位符？
3. **小程序用户**（openid 不以 `uin:` 开头）有没有带上 `mobile` 字段？
4. **详情页链接**用的是 `data.wechat_page_url` 里的路径，不是我自己编的？
5. **非 200**时有没有把 `message` 字段原文告知用户？

任一项答 No，**回去重新组织**。

---

## 降级策略

| 情况 | 处理 |
|------|------|
| 接口超时 / 网络失败 | 告知"资质服务暂时不可用，请稍后再试"，不编造数据 |
| 非 200，message 含"查询不到企业名" | 提示用户核实全称，可以重新提供后再查 |
| 小程序用户但手机号为空 | 先不传 mobile 尝试，失败再请用户提供手机号 |
| 用户想要深度分析 | 告知可启动 `qualification-check` 完整 SOP 流程 |
| 用户想找代办服务商 | 转 `commodity-recommend`，关键词"资质申报" |

{{/if}}
