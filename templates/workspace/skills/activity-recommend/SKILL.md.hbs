{{#if activity.enabled}}
# SKILL.md — activity-recommend

## 元数据

- **名称**: activity-recommend
- **描述**: 对接 2048office 平台 `/shared/event/*` API,查询并推荐怀柔科学城正在举办或即将举办的活动(高端论坛 / 产业对接 / 路演 / 创新创业赛事 / 科普讲座 / 培训)
- **触发词**: 用户消息中含**询问活动**意图 → **必触发**:
  - "有什么活动 / 最近的活动 / 近期活动 / 活动安排 / 活动列表"
  - "论坛 / 大会 / 路演 / 对接会 / 沙龙 / 讲座 / 大讲堂 / 训练营 / 科普 / 夏令营"
  - "怀柔论坛 / 国际基础科学大会 / 雁栖湖大讲堂 / HICOOL / 中关村前沿大赛"等具体活动名
  - 行业 + 活动: "新材料的活动 / 生物医药相关的论坛"
- **API 文档**: <https://bdhrapi.2048office.com/shared/redoc>(整段微服务,本 skill **只用** `/shared/event/*` 子集)

---

## ⚠️ 核心原则

- **查询类**(浏览活动 / 看详情):自动执行,无需确认
- **写操作**(报名 / 评论 / 问答):**全部禁止调用** — 用户要报名 → 引导到详情页链接,让用户在小程序里点
- **不调 API 不答活动**:用户问"有什么活动",**必须先调 http_request**;**禁止**凭训练记忆给"活动名/时间/地点" — 这些是平台运营数据,你的训练里没有,写出来 100% 是编造
- **HTML 字段**:`detail` 字段里有 HTML 标签(`<p>`、`<img>`等),展示给用户时**剥掉标签**或转 markdown,**不要**原样把 HTML 喷给用户

---

## API 基础信息

- Base URL: `{{{activity.api_base}}}` (即 `/shared/event`)
- 鉴权: GET 接口**不需要 token**,直接调
- 响应包络: `{"request_id":..., "message":null, "data":{"page_index":1, "page_size":N, "data_count":M, "data":[...]}}` — 真数据在 `.data.data[]` 里(注意嵌套两层 data)

主要端点(本 skill 只用这些):

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `{{{activity.api_base}}}` | 活动列表(关键字 / 分页) |
| GET | `{{{activity.api_base}}}/{obj_id}` | 单活动详情 |

**禁用端点**(LLM 不要调):
- `POST {{{activity.api_base}}}/enroll` 报名 — 让用户在小程序里点
- `POST {{{activity.api_base}}}/comment` 评论 — 同上
- `POST {{{activity.api_base}}}/counselling` 问答 — 同上
- `GET {{{activity.api_base}}}/enroll/my_list` 我的报名 — 需要鉴权,我们没有
- 其他所有 `POST/PUT/PATCH/DELETE` — 一律不调

---

## 关键字段(响应里出现,记住其语义)

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` | int | 活动 id,详情链接用 |
| `name` | str | 活动名(显示) |
| `detail` | str (HTML) | 详细介绍,**展示前剥标签** |
| `organizer` | str | 主办方 |
| `sponsor` | str | 协办/赞助方("无"或具体单位) |
| `venue` | str | 场地(如"城市客厅 B 区 N6 栋 1 层会议室") |
| `start_time` / `end_time` | ISO datetime | 活动起止 |
| `deadline` | ISO datetime | 报名截止 |
| `event_type` | enum | 活动分类(如 `INDUSTRIAL` 产业对接、其他枚举值看返回) |
| `enroll_type` | enum | 报名类型(`OUTER` 对外开放、其他) |
| `enroll_number` | int | 报名上限 |
| `enroll_count` | int | 已报名人数 |
| `sign_count` | int | 已签到人数 |
| `view_count` | int | 浏览量 |
| `comments_score` | float | 评论平均分(0–10) |
| `is_top` | bool | 是否置顶 |
| `status` | enum | `ONLINE`/`OFFLINE` 上线状态(只展示 ONLINE) |
| `audit_status` | enum | `APPROVED`/其他 审核状态(只展示 APPROVED) |

---

## 执行流程:5 步自主售前

### Step 1:理解需求,确定关键词

| 用户诉求 | keyword 参数 |
|---------|-------------|
| 论坛 / 大会 / 学术 | `论坛` 或 `大会` |
| 路演 / 对接 / 招商 / 成果发布 | `路演` 或 `对接` |
| 创新 / 创业 / 大赛 / 比赛 | `大赛` 或 `创业` |
| 科普 / 讲座 / 夏令营 / 大讲堂 | `讲座` 或 `科普` |
| 培训 / 工作坊 | `培训` |
| 用户行业相关 | 用户行业关键词(如"新材料"、"生物医药"、"人工智能") |
| 用户提到具体活动名 | 直接用活动名作 keyword |

### Step 2:搜索活动(自动执行)

#### 2a 用户给了关键词

```
http_request GET {{{activity.api_base}}}
  ?keyword=<速查表的值>
  &page_size=10
  &page_index=1
```

API 关键词检索会全表扫,所以无需翻页 — 一次拿到的就是全部命中。

#### 2b 用户只问"最近有什么活动"没具体方向 ⚠️ 必须翻最后一页

**重要 — API 默认按 id 升序返回(老活动在前),最近的活动都在最后一页。直接拉 page_index=1 会拿到一堆已结束的旧活动,然后被 Step 3 的 end_time 过滤砍光,导致错误回答"没有活动"。**

正确流程:

1. 第一调:读总数
   ```
   http_request GET {{{activity.api_base}}}?page_size=10&page_index=1
   ```
   从响应里取 `data.data_count`(假设 = N)。

2. 计算最后一页 `last = ceil(N / 10)`

3. 第二调:拉最后一页(里面是最新创建的活动)
   ```
   http_request GET {{{activity.api_base}}}?page_size=10&page_index=<last>
   ```

4. 对最后一页的 `.data.data[]` 做 Step 3 过滤;如果 future + ONLINE + APPROVED 的不到 5 条,继续拉 `page_index=<last-1>`,合并去重再过滤。最多翻到 last-2 就停(再往前都是更老的,没必要)。

5. 第一调读出来的 page=1 数据可以直接丢弃(那是最老的一批),只用它的 `data_count` 字段。

### Step 3:可行性校验(过滤候选)

对返回的 `.data.data[]`,只保留满足以下**全部**条件的:

1. `status == "ONLINE"` 且 `audit_status == "APPROVED"`
2. `end_time` 不早于今天(已结束的活动一般不推,除非用户明确问历史)
3. `name` 跟用户诉求**真实相关**(光含关键字不够,要语义相关)
4. 如果有多条 `is_top == true`,优先排前面

留 TOP 5。报名快满 / 已截止报名(`enroll_count >= enroll_number` 或 `deadline < now`)的活动**也展示**,但要在简介里加"⚠️ 报名已满 / 报名已截止",不要让用户白跑。

### Step 4:展示推荐清单

格式严格按下面模板,每条活动**一段**:

```
**N. <name>**
- 时间:<start_time YYYY-MM-DD HH:MM> – <end_time HH:MM>(报名截止 <deadline 月日>)
- 地点:<venue>
- 主办:<organizer>
- 简介:<detail 剥 HTML 截到 60 字内,带省略号>
- 参与情况:已报名 <enroll_count>/<enroll_number>(可选: ⚠️ 报名截止 / 已满)
- [查看详情并报名](该活动 id 替换到路径模板里的链接)
```

链接路径模板: `{{{activity.detail_path_template}}}` — 把 `{id}` 换成 API 返回的 `id` 数字。比如 id=72 → 链接目标 `/pages/activity/details/Index?obj_id=72`。

末尾固定加:

```
要看哪个活动的详情或对比?告诉我即可。
活动报名请到详情页操作,我这里不直接代办报名。
```

### Step 5:用户追问详情(按需执行)

用户指 "第 N 个 / 启动仪式那个 / id=72 详情":

```
http_request GET {{{activity.api_base}}}/<obj_id>
```

返回单活动完整 schema。整理给用户:
- 时间地点报名情况(同 Step 4)
- 详细介绍(从 `detail` 剥 HTML 后给完整内容,长则分段)
- 主办/承办/协办全部列出
- 报名截止 + 当前已报人数 + 余位
- 强调"详情页有报名按钮,请到小程序内点击 [链接] 报名"

**禁止**回到 Step 4 重新搜 — 用户已经选定了。

---

## 自检(回答前必过)

1. **我列的活动 id**,是不是来自这一轮 `http_request` 响应的 `data.data[].id`?如果不是,**不要回答** — 必须先调 API。
2. **我列的时间/地点**,是不是 API 响应里的 `start_time`/`end_time`/`venue`?**禁止**编造或拼凑。
3. **我的链接** `[查看详情](path)` 里,`path` 是不是把 `{id}` 换成了**真 id 数字**?
4. **`detail` 字段**有没有 HTML 标签残留?如果有,**回去剥**再展示。
5. **报名相关**有没有调 POST?如果调了,**回去删** — 我们只引导用户去小程序自己点。
6. **无关键词查询**(用户问"最近有什么活动")时,我有没有按 Step 2b 翻到**最后一页**?如果只调了 page_index=1 就回答"没活动",**回去重做** — 那一页是最老的活动,被 end_time 过滤掉是正常的,不代表没有近期活动。

任一项答 No,**回去重新执行**。

---

## 降级策略

| 情况 | 处理 |
|------|------|
| API 401/403 | 不应该发生(GET 免鉴权)。真发生了 → 告诉用户"活动列表暂时不可用",**不**编造活动 |
| 搜无结果(`data_count=0`) | 换 keyword 重试 1 次(用更宽的词);仍无 → 告诉用户"暂无符合条件的活动,可以告诉我具体感兴趣的方向再试" |
| 网络超时(`http_request` 失败) | 告诉用户"活动平台暂时取不到,稍后再试",**不**编造 |
| 用户问"帮我报名 / 直接报名" | 回复"报名要到活动详情页操作,我这里不代办;[链接] 进去点报名按钮即可" — **不调** POST enroll |
| 用户问"我之前报过的活动" | "我这边查不到你的报名记录(需要小程序登录态),请到小程序'我的活动'查看" — **不调** my_list |
| 全部活动都过期(end_time 早于今天) | 告诉用户"近期暂无符合条件的活动,你可以关注 '怀柔科学城' 公众号或 '科服伙伴' 小程序的科创活动板块获取最新通知" |

---

## 与其他技能的边界

- 用户问**活动相关代办服务**(展会代运营、路演代办) → 走 `commodity-recommend` 找平台代办产品,**不**用本技能
- 用户问**怀柔科学城整体介绍 / 重点活动有哪些是常态化品牌**(国际基础科学大会、怀柔论坛、雁栖湖大讲堂这种"年度品牌"概念) → 走 `general-information` 第十一节
- 用户问**具体某场活动的现状**(下一届何时办、今年办几次、报名人数) → 走本技能调 API
- 用户问**会议室预约 / 体育场馆预约**(虽然 `/shared` 服务里有这些端点) → **不**在本 skill 范围内,告知用户去 "雁栖云客厅" 小程序操作
{{/if}}
