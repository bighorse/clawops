{{#if commodity.enabled}}
# 平台服务产品推荐(commodity-recommend)

⚠️ **这是强制规则,不是参考资料**。

用户表达任何采购意图时(关键词:"我想找 / 我需要 / 帮我推荐 / 有没有 / 给我看 XX 服务/产品/代办/方案"),你**必须**:

1. **第一步**:调 `http_request` GET 平台 commodity API(见下面具体 URL)
2. **第二步**:用 API 返回的真实数据生成推荐清单
3. **禁止**在没调 API 的情况下输出公司名 / 服务名 / 价格 / 周期 —— 这些是平台运营数据,**你的训练记忆里没有**,**写出来 100% 是编造**,会误导用户决策。这一条没有例外。

**自检**:回答前先问自己:"我列的产品名,是不是从这一轮 http_request 响应里读出来的?" 如果答案是 No,**不要回答** —— 必须先调 API。

## API 入口

- API 根路径(常量,渲染时已注入):见下面 case 里的 URL
- 鉴权:GET 接口**无需 token**,直接调
- 响应包络:`{"request_id": ..., "message": null, "data": {...}}`,真实数据在 `.data` 字段里

## 四种使用场景

### 场景 1:关键字搜索(最常用)

触发例:"我想找招聘服务" / "有没有商标注册的产品" / "帮我找工商注册"

调用:GET 请求至 `{{commodity.api_base}}/services/products`

查询参数:
- `keyword`(string)模糊匹配,多个用英文逗号分隔
- `category_ids[]`(int 数组)按分类过滤
- `service_scope[]`(string 数组)服务地域,如 `["北京"]`
- `is_innovation_coupon_service`(int,1 = 只看创新券产品)
- `page_index`(默认 1)、`page_size`(默认 20,推荐传 10)

响应 `.data.data[]` 每项含:`id`, `service_name`, `description`, `min_price`, `max_price`, `shop_name`, `images[]`, `view_count`。

### 场景 2:推荐清单(平台运营推荐)

触发例:"业务刚起步有什么推荐" / "新手入门必备" / "热门服务"

调用:GET 至 `{{commodity.api_base}}/services/products/recommend_list?page_size=10`

响应字段同场景 1。

### 场景 3:浏览分类(产品太多时先收敛话题)

触发例:"你们都有什么类型的服务" / 用户没具体关键字时

调用:GET 至 `{{commodity.api_base}}/categories?tree=true&page_size=50`

响应 `.data.data[]` 含:`id`, `name`, `child_categories[]`(树形)。把分类名抛给用户选,再用对应 `category_id` 调场景 1。

### 场景 4:看具体产品详情

触发例:用户提到具体产品名/ID,或在前面搜索结果里追问"第 X 个具体怎么收费"

调用:GET 至 `{{commodity.api_base}}/services/products/<obj_id>`(把 `<obj_id>` 换成产品 id)

响应是单个产品的完整 schema(规格、详细介绍、图片等)。

## 输出给用户的格式

最终回复给用户用 **markdown 列表**,每项必含:**产品名(带链接)、价格区间、店铺、一句话简介**。

**链接规则**:用 markdown 链接语法 `[产品名](path)`,其中 `path` 用本路径模板,把 `{id}` 替换成 API 响应中实际的 `id` 值:

  路径模板(原样,不要改):`{{commodity.detail_path_template}}`

举例,产品 id 是 1234,产品名"翰翼",输出形如:**翰翼**接 markdown 链接,链接目标是 `{{commodity.detail_path_template}}` 把 `{id}` 替换成 `1234`。**不要**写成 `{{commodity.api_base}}/...` —— 那是 API 不是详情页,用户点了打不开。

输出准则:
- 默认返 5 条,够用即可。用户说"多看几个"才上 10
- 每项 1-2 行,**不要**把 description 全文复制(可能几百字)
- 价格区间用"¥min–max"或"¥min 起",**不要**写 `null`(API 偶尔返回 null,过滤掉这个字段)
- **没有结果**时:告诉用户当前关键词没匹配到,建议换词或先看分类(场景 3)
- **API 返回错误**(非 200):告诉用户"产品库暂时取不到,稍后再试",**不要**编造产品

## 何时**不**用本 SKILL

- 用户问"政策"/"补贴"/"申报窗口"——这是 web_search 找政策原文,不是平台产品
- 用户已在结账/下单流程——本 SKILL 只做推荐,**不**走订单 API(POST 接口)
- 用户问"我的订单状态"——本 SKILL 也不查订单(订单接口需要 Bearer 鉴权,我们没有)

## 安全 & 限制

- 只调 GET 接口。**禁止**调 POST/PUT/PATCH/DELETE(写操作需要 Bearer token,我们没有,而且会动平台数据)
- 只调 `{{commodity.api_base}}` 域下的 URL,**不要**猜其他 endpoint
- 一次对话里 `http_request` 调用 ≤ 3 次,避免无限刷接口
{{/if}}
