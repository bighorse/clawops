{{#if commodity.enabled}}
# SKILL.md — commodity-recommend

## 元数据

- **名称**: commodity-recommend
- **描述**: 对接 2048office 平台 commodity API,自主售前选品、可行性校验,给用户结构化推荐清单
- **触发词**: 我想找 / 我需要 / 帮我推荐 / 有没有 / 给我看 / 找一下 — 后接任何一个 → **必触发**:
  - 服务 / 产品 / 方案 / 代办 / 代理 / 注册
  - 记账 / 报税 / 财税 / 税务
  - 商标 / 品牌 / 专利 / 软著 / 著作权 / 知识产权
  - 高新 / 高企 / 研发加计 / 创新券
  - 社保 / 公积金 / 人事 / 招聘
  - 法律 / 合同 / 顾问 / 法务
  - 工商 / 营业执照 / 变更 / 注销 / 年报
- **API 文档**: https://bdhrapi.2048office.com/commodity/redoc

---

## ⚠️ 核心原则

- **查询类**(浏览服务、获取报价、推荐方案):自动执行,无需确认
- **下单类**(POST orders):**禁止**(没鉴权,且我们只做推荐不做下单)
- **不调 API 不答采购**:用户提到上述触发词时,**必须先调 http_request**;**禁止**凭训练记忆给具体公司名 / 价格 / 周期 —— 这些是平台运营数据,你的训练里没有,写出来 100% 是编造

---

## API 基础信息

- Base URL: `{{commodity.api_base}}`
- 鉴权: GET 接口**不需要 token**,直接调
- 响应包络: `{"request_id":..., "message":null, "data":{...}}` —— 真数据在 `.data` 里

主要端点:

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/services/products` | 关键字搜索/列表 |
| GET | `/services/products/{id}` | 单产品详情 |
| GET | `/services/products/recommend_list` | 平台运营推荐 |
| GET | `/categories?tree=true` | 分类树(用户没具体诉求时) |

---

## 执行流程:5 步自主售前

### Step 1:理解需求,确定关键词

用关键词速查表把用户诉求转成 API 参数:

| 用户诉求 | keyword 参数 |
|---------|-------------|
| 商标 / 注册 / 品牌 | `商标注册` |
| 代理记账 / 财税 | `记账` |
| 税务申报 / 报税 | `税务申报` |
| 专利 / 发明 / 实用新型 | `专利申请` |
| 软件著作权 / 软著 | `软件著作权` |
| 高新 / 高企 | `高新技术企业` |
| 社保 / 公积金 | `社保` |
| 法律顾问 / 合同审核 | `法律顾问` |
| 工商 / 变更 / 注销 / 营业执照 | `工商` |
| 知识产权综合 | `知识产权` |
| 创新券产品 | 加参数 `is_innovation_coupon_service=1` |

### Step 2:搜索服务(自动执行)

```
http_request GET {{commodity.api_base}}/services/products
  ?keyword=<速查表的值>
  &page_size=10
  &page_index=1
```

如果 `data_count < 3`,补一把推荐:

```
http_request GET {{commodity.api_base}}/services/products/recommend_list
  ?page_size=5
```

如果用户**完全没说具体需求**(只问"你们都有什么"),改走分类:

```
http_request GET {{commodity.api_base}}/categories?tree=true&page_size=50
```

### Step 3:可行性校验(过滤候选)

对返回的 `.data.data[]`,只保留满足以下**全部**条件的:

1. `service_name` 跟用户诉求**真实相关**(光含关键字不够,要语义相关 —— 比如 "商标注册" 通过,"商标侵权诉讼" 不通过,因为用户要"注册"不要"诉讼")
2. `min_price` 和 `max_price` **不全是 0 或 null**(为 0/null 一律记为"价格面议",不要造价)
3. `description` 不为空

留 TOP 5。如果过滤后 < 2 条,告诉用户"暂无强匹配,这是接近的产品",再列出来。

### Step 4:展示推荐清单

格式严格按下面模板,每条产品**一段**:

```
**N. <service_name>** — <shop_name>
- 价格:¥<min_price>–<max_price>/件 (price 全为 0 时写"面议")
- 简介:<description 截到 60 字内,带省略号>
- [查看详情](该产品 id 替换到路径模板里的链接)
```

链接路径模板: `{{commodity.detail_path_template}}` —— 把 `{id}` 换成 API 返回的实际 `id` 数字。比如 id=370 → 链接目标 `/pages/products/detail?id=370`。

末尾固定加:

```
如需了解某个产品的详情或对比服务差异,告诉我即可。
```

### Step 5:用户追问详情(按需执行)

用户指 "第 N 个产品" / "翔石那个怎么样" / "id=370 详情":

```
http_request GET {{commodity.api_base}}/services/products/<obj_id>
```

返回单产品完整 schema,把 `specs`(规格列表)、`shop`(店铺/联系)、`deliverables`(交付物)整理给用户。**禁止**回到 Step 4 重新搜 —— 用户已经选定了。

---

## 数量类问题(多少款 / 多少家 / 几个分类) — 标准答法

⚠️ 用户问"你们平台**有多少**项服务/几款产品/多少家服务商/几个分类",**不要**走 Step 1-5 的"列清单"流程,这里是数数,不是推荐。**也不要**凭训练记忆胡报,API 返回的 `data_count` 才是真。

每种"数量"对应**不同端点**,选错一个数字就是错的。速查表:

| 用户问什么 | 调哪个端点 | 拿哪个字段 | 注意 |
|-----------|-----------|-----------|------|
| 多少**项服务 / 多少款产品 / 几个服务**(意图模糊但隐含产品计数) | `GET {{commodity.api_base}}/services/products?page_size=1&status=ONLINE` | `data.data_count` | 默认只数**已上线**(status=ONLINE) — 草稿/下架/审核中不算 |
| 多少**家服务商 / 入驻商户 / 供应商** | `GET {{commodity.api_base}}/shops?page_size=1&status=ONLINE` | `data.data_count` | 同上,只数 ONLINE — 不要把 CHECKING/REJECT 算进去 |
| 多少**个一级分类 / 几个方向** | `GET {{commodity.api_base}}/categories?tree=true&page_size=50` | **响应里 `name` 字段去重后的个数** | ⚠️ **不要直接用 `data_count`** — 该 API 当前数据脏,同名分类有重复行(实测 20 行 / 9 个不同名),`data_count` 拿到的是行数不是真分类数 |

调用之后**必须**:

1. 直接用 API 返回的精确数字回答,**不要**做除法/估算/四舍五入凑整数
2. 报数字之前在心里过一遍正文清单条数 — 如果你列了 9 个分类却说"20 个",就是数据矛盾,**回去检查**
3. 如果用户的诉求模糊("有多少服务"),优先按"产品数"答,再补一句"如果你想知道按分类有几个方向,我可以再拉一份"
4. **禁止**回答"差不多 X 个"或"大约 X 多" — API 给了精确数就报精确数

回答模板:

```
平台目前**已上线** XXX 款企业服务,来自 YY 家服务商,覆盖 N 个一级分类
(成果转化、政策服务、商务服务、科技金融、科技资源、科学城果、供需对接、
聚合服务等)。需要我按某个分类列具体产品吗?
```

(数字用 API 真实值,分类清单从 categories 响应去重后取。)

---

## 自检(回答前必过)

1. **我列的产品 id**,是不是来自这一轮 `http_request` 响应的 `data[].id`?如果不是,**不要回答** —— 必须先调 API。
2. **我列的价格**,是不是 API 响应的 `min_price`/`max_price`?如果是 0/null,我有没有写"面议"?
3. **我的链接** `[查看详情](path)` 里,`path` 是不是把 `{id}` 换成了**真 id 数字**?
4. **数量类回答**:我报的总数字,是不是对应**正确的端点**?(产品 vs 商家 vs 分类是三个不同的 API,数字不能混用)
5. **数量类回答**:我报的数字,跟我同时列出来的清单条数**对得上**吗?对不上(比如说"20 个分类"但只列出 9 个)→ **回去重数**,数据有重复要去重。

任一项答 No,**回去调 API,不要凭记忆出答**。

---

## 降级策略

| 情况 | 处理 |
|------|------|
| API 401/403 | 不应该发生(GET 免鉴权)。真发生了 → 告诉用户"产品库暂时不可用",**不**编造产品 |
| 搜无结果(`data_count=0`) | 换 keyword 重试 1 次(用上下文里更宽的词);仍无 → 转 `categories?tree=true` 拉分类问用户具体方向 |
| 网络超时(`http_request` 失败) | 告诉用户"产品库暂时取不到,稍后再试",**不**编造 |
| 用户问下单/购买流程 | 回复"我可以帮你找产品和对比方案,具体下单请到产品详情页操作" —— **不调** POST |

---

## 用户留资(留下联系方式 → 通知运营群)

用户在选品/对比之后进入"想直接联系服务商 / 想要详细报价 / 想下单 / 想让顾问对接"等场景时,**主动建议**留个联系方式。当用户**给出**手机号 / 微信号 / 邮箱,**必须**做以下两步,不能只默认"我心里记下了":

### 触发条件(任一发生即进入留资流程)

- 用户问"能不能直接联系服务商 / 商家电话 / 联系方式"
- 用户问"具体多少钱 / 能不能便宜 / 谈价"
- 用户问"怎么下单 / 怎么签约 / 怎么付款"
- 用户已经选定一款产品(说"就这个 / 我想买 X"),需要进一步对接
- 用户主动说"让顾问联系我 / 给我留个对接人"

→ 此时回:**"我帮你登记一下,运营顾问会主动联系你。麻烦留一个手机号或微信号 + 你的称呼,以及你最关心的诉求。"**

### 用户给了联系方式后 — 必须按顺序做以下两步

**第一步:本地存档** — 调 `memory_store`:
- `key`: `commodity-lead-<联系方式末四位 或 微信号>-<日期 YYYYMMDD>`(保证唯一)
- `content`: 一段简洁陈述,包含**称呼 + 联系方式 + 意向产品 + 意向商家 + 诉求摘要**
- `category`: `"lead"` (固定)

{{#if lead.enabled}}
**第二步:实时通知运营群** — 调 `http_request` POST 飞书机器人 webhook:

```
url: {{lead.webhook_url}}
method: POST
headers: {"Content-Type": "application/json"}
body: {
  "msg_type": "text",
  "content": {
    "text": "🛒 企服留资\n意向产品:[服务名,如有具体产品则带上]\n意向商家:[商家名,如有]\n联系人:[姓名 / 称呼]\n联系方式:[手机号 / 微信号 / 邮箱]\n用户企业:[memory 里的 user_profile_company_name,无则写 未填]\n行业:[memory 里的 user_profile_industry,无则写 未填]\n核心诉求:[从对话中提取的关键诉求,如预算 / 时效 / 特殊要求]\n时间戳:[YYYY-MM-DD HH:MM]"
  }
}
```

**调用规则**:
- `msg_type` 必须是 `"text"`(飞书自定义机器人只稳定接受 text)
- 文本里的所有 `[xxx]` 占位符**必须替换成真实内容**,不要原样保留方括号
- webhook 调用失败(非 200) → **不**告诉用户失败,信息已落 memory
- 通知**只发一次**,不循环重试
{{else}}
**第二步当前未启用** — 飞书 webhook 未配置,只完成第一步 `memory_store` 即可。
{{/if}}

### 给用户的回复(两步做完之后)

简短回应:
> 好的 [称呼],已经登记,运营顾问会在 1 个工作日内主动联系你。

⚠️ **不要**说"已发送通知到飞书群 / 已经推送给运维" —— 后台细节,违反 SOUL 输出契约。

### 自检(留资场景特别加强)

留资流程结束前,必须确认:
1. 我有没有调 `memory_store` 落库 category=lead?**没调就回去补**
2. (lead.enabled 为真时)我有没有调 `http_request` POST 飞书 webhook?**没调就回去补**
3. 我给用户的回复里,**有没有**出现"飞书 / webhook / 通知 / 推送 / API"这类技术词?有就**回去重写**

---

## 与其他技能的边界

- 用户问**政策 / 补贴 / 申报窗口** → web_search,**不**用本技能(政策不是平台产品)
- 用户问**订单状态 / 我的购买记录** → 不是本技能(订单查询要 Bearer 鉴权,我们没有)
- 用户问**详细业务建议**(怎么注册、流程是啥) → 先用本技能找产品,再让用户去产品详情页看,**不**自己解释流程
{{/if}}
