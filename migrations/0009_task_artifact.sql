-- 任务与其产出的简报此前毫无关联：任务表只有 task_id/状态/时间戳，简报接口
-- 只有路径/大小/修改时间，两边没有任何共同字段。用户跑完几个企业快评后，
-- 无法判断哪份简报出自哪个任务 —— 并发时尤其明显（实测两个任务、两份简报，
-- 只能靠时间戳猜，而简报落盘比任务标记完成早约半分钟，顺序还可能颠倒）。
ALTER TABLE sop_tasks ADD COLUMN artifact_path TEXT;
