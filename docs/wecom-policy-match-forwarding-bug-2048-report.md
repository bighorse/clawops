# 政策匹配"不出卡片"排查报告 — 给 2048 平台方

**日期**：2026-07-01
**渠道**：企业微信机器人「AI政策顾问 @百维互联」
**受影响账号**：`uin:7881303049925005`（马广军）
**结论一句话**：该用户账号的企微消息**当前没有被转发到 clawops/zeroclaw**，而是由 2048 企微后端自身处理；政策匹配意图被后端回以"报告生成中…主动发送"的话术，但**从不实际产出报告/卡片**。问题在 **2048 企微后端 → clawops 的转发链路**，不在 clawops/zeroclaw。

---

## 一、现象

用户在企微里请求政策匹配（例："为百维互联科技发展(北京)有限公司做个政策匹配"），机器人回复：

> "您好，已收到您的查询需求。正在为「百维互联科技发展(北京)有限公司」检索并整理相关企业信息，报告生成大约需要 1-3 分钟，生成好后我会主动发送给你"

之后**报告/小程序卡片从不到达**。

## 二、定位证据（clawops 侧数据库 + 日志，时间为 UTC）

| 检查项 | 结果 |
|---|---|
| 用户政策匹配请求（07-01 02:06 UTC = 10:06 CST） | clawops **无任何记录**：无 chat_messages、无 sop_tasks、无 /chat 日志、无报错 → **从未到达 clawops** |
| 同账号"帮我找记账服务"（07-01 ~02:3x UTC） | clawops **同样无记录** → 也未到达 clawops |
| 该账号最后一次到达 clawops | 06-30 09:17 UTC（测试流量）；之后至今 clawops 收到该账号 **0 条** |
| 过去 60 分钟 clawops 收到的消息（所有账号） | 0 条 |
| **对照**：另一真实账号 `uin:788130190408` 07-01 00:21–00:22 UTC 的"公司注册/记账"查询 | **成功到达 clawops 并获得真实回答** → clawops 接收管道正常 |
| 该用户账号 06-30 13:04 CST 的政策查询 | 曾**成功到达 clawops 并完成** → 该账号此前可转发，约 06-30 下午起停止 |

**推断**：2048 企微后端当前未把该账号的微信消息转发给 clawops。后端自行处理——commodity 类能给真实推荐，政策匹配类给"报告生成中…"话术但不交付。转发中断时间点与 clawops 服务当日多次重启的时段吻合，疑似**转发链路熔断/会话失效后未恢复**，或**按账号/按意图的路由**未覆盖政策匹配。

## 三、clawops/zeroclaw 侧状态（供参考，非问题所在）

- clawops `/chat` 接收正常、`/health` 健康；其它账号的查询能正常进入并获真实回答。
- 政策匹配 SOP 在**收到请求时工作正常**：约 4 分钟完成，产出小程序政策匹配页深链（近期实测 `/pages/recommendation/index?id=34`、`id=137`），并经 `save_match_result` 推送卡片。
- SOP 引擎近期已加健壮化：模型若在多步流程中途停手，会自动续推到最终步骤，避免半途而废。
- **即：只要请求被转发到 clawops，政策匹配能稳定出卡片。**

## 四、请 2048 平台方排查 / 修复

1. **为什么 `uin:7881303049925005` 的企微消息当前不再转发到 clawops `/chat`？**（而 `uin:788130190408` 今天可以）
   排查方向：该账号转发链路是否中断、clawops 重启期间是否触发熔断且未恢复、会话/鉴权令牌是否失效、是否存在按账号或按意图的路由分支。
2. **政策匹配意图应转发给 clawops**（那边 SOP 能稳定产出卡片），不要由后端自身的"报告生成"流程处理——该流程只回"报告生成中…主动发送"却从不交付。
3. **在没有真实产出时，不要向用户发出**"报告生成大约需要 1-3 分钟，主动发送给你"这类承诺（用户会一直空等）。

## 五、附：clawops 接收契约（供转发实现参考）

- 端点：`POST http://<clawops_host>:8088/chat`
- 头：`Authorization: Bearer <该用户 session_token>`、`Content-Type: application/json`
- 体：`{"content": "<用户原始消息>"}`
- 政策匹配行为：clawops 立即返回 `"正在为您处理「政策匹配」，请稍候，完成后会主动通知您。"`；SOP 异步跑完后，结果经平台 `save_match_result` 接口推送为小程序政策匹配页卡片。
